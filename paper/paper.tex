%% BinML methods paper -- AASTeX 7.0.1
%% Build with ./build.sh (regenerates macros + figures, then pdflatex/bibtex).
\documentclass[twocolumn,linenumbers]{aastex701}

\input{paper_macros}   % \bml* numbers, auto-generated from canonical_numbers.json

\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\dchisq}{\ensuremath{\Delta\chi^2}}
\newcommand{\Rom}{\textit{Roman}}

\shorttitle{BinML: a real-time classifier for Roman microlensing}
\shortauthors{Bhatia}

\begin{document}

\title{BinML: A Real-Time, Detectability-Conditioned Deep-Learning Classifier
for the \textit{Roman} Galactic Bulge Time-Domain Survey}

\author{Kunal Bhatia}
\affiliation{Independent Researcher}
\email{muffin29011@gmail.com}

\begin{abstract}
The Nancy Grace \Rom{} Space Telescope Galactic Bulge Time-Domain Survey (GBTDS) will monitor
hundreds of millions of stars at $\sim$15-minute cadence, producing a microlensing data stream
too large to vet by hand and one in which the science-critical events---binary and planetary
lenses---reveal themselves only through brief caustic anomalies superimposed on an otherwise
ordinary magnification. We present \textbf{BinML}, a compact ($\bmlNparams$-parameter)
convolutional-stem-plus-transformer classifier that sorts \Rom{} light curves (three photometric
bands) into six physically-meaningful classes: no detectable event (\texttt{Flat}), single-lens
microlensing (\texttt{PSPL}), anomalous binary/planetary microlensing (\texttt{NonPSPL}), and
three variable-star contaminant populations (\texttt{PeriodicVar}, \texttt{LongPeriodVar},
\texttt{Eruptive}). Two ideas set BinML apart from a conventional classifier, both aimed at
survey deployment rather than benchmark accuracy. The first is \emph{detectability-conditioned
labelling}: an event is labelled by what is \emph{observable} in the photometry, not by the
parameters used to generate it, so a buried anomaly is honestly a single lens and the network is
never trained to assert evidence that is not present. The second is a \emph{real-time cascade}:
by keying each label on a per-event anomaly-onset time, the model reports
\texttt{Flat}$\rightarrow$\texttt{PSPL}$\rightarrow$\texttt{NonPSPL} only as each class's
evidence arrives, cutting premature binary alerts from $\bmlPrematureBefore\%$ to
$\bmlPrematureAfter\%$ without sacrificing full-season discrimination. On an independent
$\bmlNtest$-event test set BinML reaches a completeness of $\bmlCompleteness$ at a fixed anomaly
purity of $\bmlPurityTarget$ (average precision $\bmlAP$), recovering almost all of the oracle
anomaly-significance ranking (AP $\bmlAPoracle$, computed from the noiseless truth) directly from
raw photometry in a single forward pass and without per-event model fitting. A
$\bmlStressN$-million-event stress test on parameter draws the model provably never saw
reproduces these numbers (macro-F1 $\bmlStressMacroF$), with degradations only at physically
extreme corners. BinML and its full simulation-to-inference pipeline are released as open source.
\end{abstract}

\keywords{Gravitational microlensing (672) --- Exoplanet detection methods (489) ---
Neural networks (1933) --- Time domain astronomy (2109) --- Convolutional neural networks (1938)}

\section{Introduction}\label{sec:intro}

Gravitational microlensing occupies a niche no other planet-detection method reaches. Because it
responds to a lens's mass rather than its light, it is sensitive to cold, low-mass planets far
from their host stars and to unbound planetary-mass objects drifting through the Galaxy---the very
population that transit and radial-velocity surveys, biased toward hot and close-in planets,
recover least well \citep{Mao1991,Suzuki2016}. The Nancy Grace \Rom{} Space Telescope will exploit
this sensitivity systematically. Its Galactic Bulge Time-Domain Survey (GBTDS) will monitor the
crowded bulge fields at high cadence in the near-infrared, and is predicted to detect of order a
thousand bound planets and to pin down the free-floating planet mass function
\citep{Penny2019,Johnson2020,Terry2023}. The wide F146 filter samples the light curve every
$\sim\bmlCadence$~minutes; two auxiliary bands, F087 and F213, add colour information on a
$\sim$6-hour cycle.

That sensitivity is inseparable from two operational problems. The first is sheer volume: the
survey will yield hundreds of millions of light curves, within which genuine microlensing events
are a small minority and anomalous---binary or planetary---events rarer still. No team can
inspect them by hand, and the definitive step for any candidate, fitting a full binary-lens
model, is far too expensive to run on the whole stream. The second problem is timing. The
signal that betrays a planet is short: a caustic crossing lasting hours to a day, riding on top
of a smooth magnification that itself unfolds over weeks. A classifier meant to \emph{trigger}
follow-up must therefore be honest about time---it must not announce a binary during the smooth
rise, before the caustic that would justify the alert has actually been observed. A model that
guesses early is worse than useless: it spends a finite follow-up budget on planets that have
not yet revealed themselves, and may never.

Machine learning has been applied to microlensing classification before, and the trajectory is
instructive. Feature-engineered classifiers such as \textsc{MicroLIA} \citep{Godines2019} and
neural networks trained on lower-cadence ground-based data \citep{Mroz2020} established that the
problem is learnable on OGLE- and ZTF-like photometry. Studies aimed specifically at high-cadence,
\Rom{}-like sampling \citep{Zhao2021} and at early, real-time recognition \citep{Early2022} are
the closest antecedents of the present work. Architecturally we build on the now-standard use of
transformers for astronomical time series \citep{Astromer2023,Vaswani2017}. What distinguishes
BinML, however, is not the network but the way the problem is posed: we treat detectability and
observation time as first-class parts of the label definition rather than as afterthoughts of
evaluation.

This paper makes three contributions, developed in the sections that follow. First, we introduce
\emph{detectability-conditioned labelling} (\S\ref{sec:sims}): events are labelled by the
evidence a perfect observer could recover, which removes the label noise that would otherwise
punish the model for failing to see what is physically unobservable. Second, we build a
\emph{real-time cascade} (\S\ref{sec:cascade}) by making each label a function of how much of the
season has been revealed, so the network learns to report classes in the order their evidence
arrives. Third, we evaluate the model honestly at survey scale
(\S\ref{sec:eval}--\ref{sec:limits}): completeness at a fixed purity as the headline metric, a
selection-aware reweighting that corrects for the training distribution, and a
$\bmlStressN$-million-event stress test on parameters the model never saw.

\section{Simulated survey and labelling}\label{sec:sims}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{outputs/figures/lightcurve_gallery.pdf}
\caption{Representative simulated \Rom{} F146 light curves, one per class, as the survey would
record them, with BinML's prediction annotated in each panel. \texttt{Flat} sources are
noise-dominated; \texttt{PSPL} shows the smooth single-lens magnification; \texttt{NonPSPL}
carries the sharp caustic features that mark a binary or planetary lens; the three
variable-star classes span short-period pulsators and eclipsing binaries (\texttt{PeriodicVar},
shown phase-folded), slow red-giant variability (\texttt{LongPeriodVar}), and outbursts
(\texttt{Eruptive}). The visual separability of the caustic signature from ordinary single-lens
curvature is precisely what the model must preserve.}
\label{fig:gallery}
\end{figure*}

\subsection{Six classes across three bands}
We simulate \Rom{} GBTDS light curves in the six classes of Figure~\ref{fig:gallery}, chosen to
span the populations a real triage pipeline must separate. \texttt{Flat} sources show no
detectable event. \texttt{PSPL} events are single-lens, point-source point-lens microlensing
\citep{Paczynski1986}. \texttt{NonPSPL} events---the anomalous, science-critical class---are
binary and planetary lenses, computed with the contour-integration code \textsc{VBBinaryLensing}
\citep{Bozza2010,Bozza2018}, and are distinguished by the caustic perturbations visible in the
third panel of Figure~\ref{fig:gallery}. The remaining three classes are the variable-star
populations that dominate the real bulge and are the classifier's most dangerous contaminants:
\texttt{PeriodicVar} (RR~Lyrae, eclipsing binaries, $\delta$~Scuti), \texttt{LongPeriodVar}
(Miras, semiregulars, and OSARGs, whose slow rise most readily mimics a long-timescale
microlensing event), and \texttt{Eruptive} (dwarf novae and Be outbursts). Event-timescale,
mass-ratio, and separation priors follow the measured bulge distributions
\citep{Mroz2019,Suzuki2016}; the variable-star templates follow the OGLE bulge catalogues
\citep{Soszynski2013,Soszynski2016}. Every event is realised on the true \Rom{} cadence in all
three bands and degraded by survey-realistic photometric noise, blending, and interstellar
extinction, so that the training distribution matches what the telescope will actually deliver.

\subsection{Labelling by what is observable}\label{sec:detcond}
The methodological core of BinML is a single principle: \emph{an event's label is set by what a
perfect observer could recover from the photometry, not by the parameters we injected}. The
principle has teeth at both boundaries the classifier cares about. A microlensing event whose
peak falls outside the 72-day season, or whose amplitude is buried beneath the noise, is labelled
\texttt{Flat}, because no algorithm and no human modeller could distinguish it from a constant
source; training the network to call it \texttt{PSPL} would only teach it to guess. Likewise, a
binary whose caustic perturbation lies everywhere below the noise floor is, observationally, a
single lens, and is labelled \texttt{PSPL}. Concretely, we quantify the anomaly with the
\dchisq{} between the true, noiseless light curve and its best single-lens fit; an anomaly whose
\dchisq{} fails a detection threshold does not earn the \texttt{NonPSPL} label. This does two
things at once. It removes the label noise that otherwise contaminates the decision boundary,
and it keeps the reported precision meaningful: an anomaly the model ``misses'' under this scheme
is one that carried no recoverable evidence in the first place. We return to the consequences of
this choice for the precision numbers in \S\ref{sec:results}.

\section{Model}\label{sec:model}

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{outputs/figures/schematic.pdf}
\caption{BinML architecture. Each of the three \Rom{} bands is binned onto a fixed grid whose
bins carry five channels---baseline-relative mean, min, and max magnitude, observed fraction, and
an observed mask. A convolutional stem, whose non-learned min/max lanes preserve caustic extrema
by construction, reduces the bands to $\bmlNtokens$ tokens that a four-layer transformer
classifies into six classes. The whole model has $\bmlNparams$ parameters.}
\label{fig:schematic}
\end{figure*}

BinML is a convolutional stem feeding a small transformer encoder (Figure~\ref{fig:schematic}),
with $\bmlNparams$ parameters in total. The compactness is deliberate. The task is not
data-starved---we generate tens of millions of events---but it is inference-heavy, since the
model must ultimately run on the entire survey stream; a small network that classifies in a
single forward pass is worth far more here than a large one that must be applied sparingly.

The input preserves the survey's own time structure. Each band is binned onto a fixed grid over
the 72-day window: F146 into $\bmlBinsF$ bins (from $\bmlEpochsF$ epochs at the
$\bmlCadence$-minute cadence), and each colour band into 96 bins. Every bin carries five channels:
the baseline-relative mean, minimum, and maximum magnitude, the fraction of the bin that was
observed, and a mask flagging whether it was observed at all. Magnitudes are expressed relative
to a single F146 baseline that is subtracted from all three bands, so that intrinsic source
colour survives the transformation and remains available to the network.

The convolutional stem is where the architecture makes its one non-negotiable commitment. As it
downsamples each band to its token count, a purely learned averaging filter would smear a
$\sim$2.9-mag caustic spike down to $\sim$0.36~mag---the amplitude of ordinary single-lens
curvature---erasing exactly the \texttt{PSPL}-versus-\texttt{NonPSPL} distinction that motivates
the entire model. We therefore reserve two stem channels as non-learned maximum and minimum
pooling lanes, carried unchanged through every downsampling step, so that the caustic extremum
survives by construction rather than at the discretion of the optimiser. The resulting
$\bmlNtokens$ tokens are processed by a four-layer, four-head pre-norm transformer
($d_{\mathrm{model}}=\bmlDmodel$). Colour bands that are absent---F087 drops out in roughly 38\%
of events under heavy extinction---are masked out of attention rather than imputed. A masked
attention-pooling layer then feeds a flat six-way classifier. The model consumes binned
magnitudes only; it is deliberately agnostic to per-point error bars.

\section{The real-time cascade}\label{sec:cascade}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\textwidth]{outputs/figures/cascade_evolution.pdf}
\caption{The real-time cascade, shown on three simulated events as the season is progressively
revealed (left to right in each panel). \emph{Top:} the F146 light curve, with the dashed line
marking the anomaly-onset day $t_{\mathrm{anom}}$ for the binaries. \emph{Bottom:} BinML's class
probability as a function of how much of the season it has seen. For the clean binary (left),
probability flows \texttt{Flat}$\rightarrow$\texttt{PSPL}$\rightarrow$\texttt{NonPSPL}, with the
jump to \texttt{NonPSPL} occurring precisely when the caustic enters the observed window---not
during the smooth rise before it. The subtle binary (centre) shows the same ordering with a late,
weak anomaly. Crucially, the single-lens control (right) never acquires appreciable
\texttt{NonPSPL} probability: the model does not invent an anomaly that is not there.}
\label{fig:cascade}
\end{figure*}

A survey classifier is never handed a finished light curve; it watches the season fill in night
by night. This is the setting in which a naive model fails in the most damaging way. A binary
lens is indistinguishable from a single lens throughout its smooth rise, and becomes identifiable
only once its caustic is on screen. A network trained on complete curves learns to
\emph{anticipate} the anomaly from the smooth portion---inflating early \texttt{NonPSPL}
probability and, in a deployed pipeline, firing follow-up triggers on planets that have not yet
revealed themselves.

We remove this failure mode by making the label itself a function of observation time. For each
binary we compute the day $t_{\mathrm{anom}}$ at which its anomaly first becomes detectable, using
the same \dchisq{} threshold that defines the \texttt{PSPL}/\texttt{NonPSPL} boundary in
\S\ref{sec:detcond}. During training the network sees light curves truncated at random days: a
binary truncated \emph{before} $t_{\mathrm{anom}}$ carries the \texttt{PSPL} label (with the
detectability floor re-evaluated on the truncated data), and earns \texttt{NonPSPL} only once the
anomaly lies inside the revealed window. The model thereby internalises the cascade
\texttt{Flat}$\rightarrow$\texttt{PSPL}$\rightarrow$\texttt{NonPSPL}.

Figure~\ref{fig:cascade} shows the cascade at the level of individual events. As the clean binary
is revealed, BinML's probability mass moves from \texttt{Flat} to \texttt{PSPL} during the smooth
magnification and only jumps to \texttt{NonPSPL} when the caustic crosses into view at
$t_{\mathrm{anom}}$; the subtle binary follows the same sequence with a weaker, later anomaly; and
the single-lens control never acquires meaningful \texttt{NonPSPL} probability at all. The
aggregate effect (Figure~\ref{fig:cascade_summary}) is large. The rate of \emph{premature} anomaly
flags---calling a binary \texttt{NonPSPL} before its anomaly is observable---falls from
$\bmlPrematureBefore\%$ to $\bmlPrematureAfter\%$, and the mean pre-onset \texttt{NonPSPL}
probability drops from $\bmlPreonsetBefore$ to $\bmlPreonsetAfter$. This costs essentially nothing
on the full-season task for which conventional classifiers are optimised: the missed-planet rate,
a true \texttt{NonPSPL} called \texttt{PSPL} on the complete curve, is statistically unchanged
($\bmlMissedBefore\rightarrow\bmlMissedAfter$).

\begin{figure}[t]
\centering
\includegraphics[width=0.82\columnwidth]{outputs/figures/cascade_summary.pdf}
\caption{Aggregate effect of the cascade over the test population. Keying the label on a per-event
anomaly-onset time cuts premature binary flags and pre-onset anomaly probability by large factors
while leaving full-season sensitivity (the missed-planet rate) intact.}
\label{fig:cascade_summary}
\end{figure}

\section{Evaluation methodology}\label{sec:eval}

Before presenting results we make explicit the three choices that make our numbers a fair reflection
of survey performance rather than of benchmark convenience.

The first is the headline metric. Accuracy and macro-F1 are poor proxies for what a follow-up
pipeline actually needs to know, which is a pair of quantities: of the anomalies it flags, what
fraction are real (the purity), and of the real anomalies, what fraction it catches (the
completeness). We therefore fix the operating threshold to deliver a target anomaly purity of
$\bmlPurityTarget$, chosen on a validation set, and report the completeness achieved there as the
headline number. This is the quantity against which a survey team would specify a triage layer.

The second is a correction for selection. The training set is not the survey population: rare and
faint events are up-sampled and abundant by-product classes down-sampled for efficiency. Each test
event therefore carries a retention probability \code{keep\_prob}, and we reweight population
statistics by $1/\code{keep\_prob}$. The reweighting is applied asymmetrically---purity and
precision are reweighted, because they depend on the population mix of false positives, whereas
recall is intrinsic to a class and is not. Every population-level number below is corrected in this
way.

The third is a reference against which to judge the network. We rank events by the injected anomaly
\dchisq{} computed from the \emph{noiseless} truth. Because it uses the true parameters, this
ranking is an oracle---it is not fieldable---and it therefore upper-bounds any blind detector. It
lets us ask not merely whether BinML is accurate, but how much of the information that is in
principle recoverable the model actually recovers.

\section{Results}\label{sec:results}

On an independent $\bmlNtest$-event test set drawn from the survey population, BinML reaches a
completeness of $\bmlCompleteness$ (95\% CI $[\bmlCompletenessLo,\bmlCompletenessHi]$) at the fixed
anomaly purity of $\bmlPurity$, with an anomaly average precision of $\bmlAP$. Per-class
performance is collected in Table~\ref{tab:perclass}, and the full confusion structure in
Figure~\ref{fig:confusion}.

\begin{deluxetable}{lccc}
\tablecaption{Per-class performance on the independent $\bmlNtest$-event test set
(population-weighted).\label{tab:perclass}}
\tablehead{\colhead{Class} & \colhead{Recall} & \colhead{Precision} & \colhead{F1}}
\startdata
Flat          & \bmlFlatR          & \bmlFlatP          & \bmlFlatF \\
PSPL          & \bmlPSPLR          & \bmlPSPLP          & \bmlPSPLF \\
NonPSPL       & \bmlNonPSPLR       & \bmlNonPSPLP       & \bmlNonPSPLF \\
PeriodicVar   & \bmlPeriodicVarR   & \bmlPeriodicVarP   & \bmlPeriodicVarF \\
LongPeriodVar & \bmlLongPeriodVarR & \bmlLongPeriodVarP & \bmlLongPeriodVarF \\
Eruptive      & \bmlEruptiveR      & \bmlEruptiveP      & \bmlEruptiveF \\
\hline
\multicolumn{3}{l}{Macro-F1} & \bmlMacroF \\
\enddata
\tablecomments{The apparently low \texttt{NonPSPL} precision ($\bmlNonPSPLP$) is dominated by
labelling, not error: $\bmlNonpsplDemoted$ of its ``false positives'' are genuine binaries whose
anomaly falls below the detectability floor and were therefore demoted to \texttt{PSPL} by the
labelling of \S\ref{sec:detcond}. Counting those as correct---they are physically anomalous---gives
a physical precision of $\bmlNonpsplPhysP$, and the recall of $\bmlNonPSPLR$ shows the model in fact
finds the anomalies.}
\end{deluxetable}

The confusion matrix in Figure~\ref{fig:confusion} makes the shape of the errors plain. The dominant
off-diagonal term is \texttt{NonPSPL}$\rightarrow$\texttt{PSPL}, and it is not misclassification in
any ordinary sense: it is overwhelmingly the detectability-floor demotion described in
Table~\ref{tab:perclass}, in which a binary with no observable caustic is labelled---and predicted---%
as the single lens it observationally resembles. The variable-star classes sit close to the
diagonal, and the most dangerous physical confusion, a \texttt{LongPeriodVar} masquerading as
microlensing, is rare.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\columnwidth]{outputs/figures/confusion.pdf}
\caption{Row-normalised, selection-corrected confusion matrix. The dominant off-diagonal,
\texttt{NonPSPL}$\rightarrow$\texttt{PSPL}, is largely the detectability-floor demotion of
Table~\ref{tab:perclass} rather than genuine error.}
\label{fig:confusion}
\end{figure}

The anomaly channel is where the model's value is decided, and Figure~\ref{fig:pr} places it against
the oracle ceiling. Ranking events by BinML's anomaly probability gives an average precision of
$\bmlAPnet$, against the oracle \dchisq{} ceiling of $\bmlAPoracle$. The gap is small: the network
recovers nearly all of the anomaly information that exists in principle, but it does so \emph{blind},
from raw photometry in a single forward pass, without fitting a binary-lens model to any candidate.
Recovery climbs monotonically with the strength of the anomaly, from $\bmlSliceSuzuki$ near the
Suzuki mass-ratio break to $0.972$ for the strongest anomalies, exactly as one would expect if the
model is limited by the information in the data rather than by its own capacity.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\columnwidth]{outputs/figures/pr_nonpspl.pdf}
\caption{Anomaly (\texttt{NonPSPL}) precision--recall. BinML (solid) tracks the oracle \dchisq{}
ceiling (dashed), which is computed from the noiseless truth and is not fieldable. The marker shows
the fixed-purity operating point adopted for the headline number.}
\label{fig:pr}
\end{figure}

Where that sensitivity lives across parameter space is resolved in Figure~\ref{fig:plane}, which
factors recovery in the $(\log s,\log q)$ separation--mass-ratio plane into three stages: whether
the survey can detect the anomaly at all, the end-to-end recovery, and the classifier's own
efficiency given that the anomaly is detectable. The classifier panel is near-uniform wherever an
anomaly is present, which means the structure visible in the end-to-end map is inherited from the
survey's detectability rather than introduced by BinML. Even deep planetary events with
$q<10^{-3}$ are recovered at $\bmlSliceDeepPlanet$.

\begin{figure*}[t]
\centering
\includegraphics[width=0.95\textwidth]{outputs/figures/efficiency_plane.pdf}
\caption{Recovery across the $(\log s,\log q)$ plane, factored into survey detectability, end-to-end
recovery, and classifier efficiency given detectability. Cells with fewer than 20 effective events
are left blank; the dotted line marks the planetary regime $q=10^{-2}$.}
\label{fig:plane}
\end{figure*}

The same picture holds along the individual parameter axes (Figure~\ref{fig:params}). Recovery
degrades gracefully toward faint baselines and weak anomalies---the two directions along which the
photometric information itself thins out---and remains high, $\bmlSliceFaint$, even for faint sources
with $m_{\mathrm{base}}>22.5$, dropping only in the noise-dominated tail.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\columnwidth]{outputs/figures/param_dependence.pdf}
\caption{\texttt{NonPSPL} recall versus source baseline magnitude (left) and anomaly \dchisq{}
evidence strength (right). The degradation tracks the loss of photometric information, not a model
pathology.}
\label{fig:params}
\end{figure}

\section{Generalisation and limits}\label{sec:limits}

A model can reproduce held-out numbers by memorising the distribution it was trained on, so we test
generalisation directly. We generated a $\bmlStressN$-million-event stress set from a disjoint
pseudo-random stream---seed base $\bmlSeedStress$ against $\bmlSeedTrain$ for the
train/validation/test data---which guarantees parameter tuples the model provably never encountered.
On the natural-population subset BinML reproduces its held-out performance, with a macro-F1 of
$\bmlStressMacroF$.

The degradations that do appear are confined to physically extreme corners, and each has a physical
cause rather than a modelling one: sources fainter than $m\approx25$, where noise begins to
manufacture spurious anomalies; very wide caustics with $s>5$, which are rarely crossed within a
single season; and sub-day timescales, which place too few epochs on the peak to constrain it. These
are near-fundamental limits of the photometry, not gaps a larger training set could close. Robustness
to missing data is likewise encouraging: events observed without the blue band are still recovered at
$\bmlSliceNoBlue$, confirming that the model degrades gracefully rather than failing when colour
information is unavailable.

\section{Discussion}\label{sec:discussion}

BinML is built to be a triage layer for the \Rom{} GBTDS: a cheap first pass that flags candidate
microlensing and anomalous events for the expensive, definitive step of full light-curve modelling.
Two properties make it suited to that role rather than merely accurate on a static benchmark. The
completeness-at-fixed-purity framing lets a survey team set the operating point directly against
their follow-up budget, trading completeness for purity along a curve they control rather than
accepting whatever an argmax delivers. And the real-time cascade means the flag can be trusted as a
\emph{trigger}: a \texttt{NonPSPL} call certifies that the anomaly has been seen, not merely
anticipated, which is the precondition for autonomous follow-up. That BinML attains near-oracle
anomaly ranking blind, in a single forward pass, is what makes running it across the full survey
stream feasible where per-event binary-lens fitting never could be.

The natural next step is validation on real events. OGLE-IV light curves, and ultimately early
\Rom{} data, are what turn ``works on simulations'' into ``works,'' and they are the trigger for any
targeted retraining. The one weak spot with real follow-up implications---faint-source false
anomalies---is the obvious target for such retraining, should real data show it to matter in
practice; the remaining limits sit close enough to the physics that we expect little return from
chasing them.

\section{Conclusion}\label{sec:conclusion}

We have presented BinML, a compact multi-band transformer classifier for \Rom{} GBTDS light curves,
built around two ideas---detectability-conditioned labelling and a real-time anomaly-onset
cascade---that make it deployable rather than merely accurate. It reaches a completeness of
$\bmlCompleteness$ at a purity of $\bmlPurity$ (average precision $\bmlAP$), recovers near-oracle
anomaly ranking blind, cuts premature binary alerts from $\bmlPrematureBefore\%$ to
$\bmlPrematureAfter\%$, and generalises to a $\bmlStressN$-million-event unseen-parameter stress
test. The model, its weights, and the full simulation-to-inference pipeline are released as open
source.

\begin{acknowledgments}
This work made use of \textsc{VBBinaryLensing} \citep{Bozza2018} and the OGLE variable-star
catalogues. Simulations were generated on transient cloud compute.
\end{acknowledgments}

\software{NumPy, PyTorch, Matplotlib, VBBinaryLensing \citep{Bozza2018}, Astropy}

\bibliographystyle{aasjournalv7}
\bibliography{refs}

\end{document}
