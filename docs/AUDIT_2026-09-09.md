# Codebase audit — 2026-09-09

Nine-agent workflow (7 auditors covering all 66 Python files + CI, 2 adversarial verifiers with refute-by-default),
followed by manual re-derivation of every paper-facing number. 79 raw findings; 2 refuted; 0 critical; 13 major; 64 minor.
**No submitted headline number is wrong.** Every artifact-backed value re-derives bit-for-bit. The defects are in
descriptions that did not match the code, comparisons that were less fair than stated, support that was smaller than
the text implied, and latent hazards that had not fired. Status column = state after the 2026-09-09 fix pass.


## Ledger — what is fixed, what is left (kept current)

**Fixed and committed (76a50de + this pass):** baseline rescored at full cadence (0.261 → 0.545, manifest updated);
`bin_band` epoch-first pooling + GULLS full-population re-score (35.6% → 11.7% false alarms, 0.50 → 0.46 recall at threshold);
OOR mix bug + swept-class support macros; F087 saturation physics (code, paper, docs); efficiency plane on test rows + regression
test; macro-F1 never-predicted class; false-anomaly bucketing; `--max-events` column subsetting; weighted prevalence line;
`make_macros` fail-closed (NaN, missing figures_stats) + worst-regress + McNemar/support macros; three tautological tests replaced;
`binml classify` requires `--m-base`; baseline-estimator bias documented; legacy CLI import + `--flux`; `modal_ablations` .done
marker + trained-epoch provenance (cascade_off verified 10/10); `modal_gap_finetune` tag guard + estimator notes;
`modal_gulls_transfer` revision pin; `gulls_transfer` cache-id guard + explicit skip rows + header numbers; docstring
corrections (gap_sensitivity, writer, pilot gate, baseline.py truthiness); REVISION.md and paper.tex passages (cadence caveat,
baseline wording, OOR support, F087, `t_anom` 7.2-d resolution, McNemar counts as macros).

**In progress:** cadence experiment rerun with held-out evaluation (`validation/cadence_local.py`).

**Left — needs a decision or an action only the author can take:**
1. Augmentation relabelling in `pipeline/train.py` (findings 8, 9, 10): `t_anom` 7.2-d quantisation, periodic amplitude proxy,
   dead Flat branch. Training labels only; no reported metric. Fixing changes `--gap-aug`/`--truncate-aug` semantics relative
   to the released checkpoints → version flag or retrain; the manuscript now describes what g08e12 actually ran with.
2. `mix_finetune.py` shard-range claim (finding 14): verify `s3://…/v5/natural/` shard indices are all < 90 after `aws login`.
3. Minor items deferred to the next artifact regeneration: `cascade_matched_reduce.py` a/b arm naming (reducer is sha-pinned),
   `agg_stress.py` weighted-vs-raw field naming, `writer.py` storing max(period) (comment corrected), `modal_latency_gaps.py`
   censoring/matching notes, legacy preprocess window edge cases, `plots.py` full-pool figures (24) and panel-C caption (20),
   `report.py` stale plane key (19), regime-name-in-seed (26).
4. Paper: regenerate figures via build.sh before resubmission (prevalence line, efficiency plane unchanged); Zenodo DOI.

| id | sev | paper | file:line | finding | verdict | status |
|---|---|---|---|---|---|---|
| 30 | major | Y | `binml/preprocess.py:87` | frac = min(v.size/factor, 1.0) uses the number of OBSERVATIONS as the numerator, not the number of distinct observed Roman epochs as pipeline.cache.bin_curve does (cnt = isfinite(r).sum(axis=2), bounded by factor), so the 'observed fraction' channel is inflated and then silently saturated whenever the input cadence is finer than the training grid. *Verifier correction:* One sub-claim is wrong: the comment at line 68 ('frac denominator, matches cache.py') is accurate — the denominator does match. The mismatch is in the numerator (observations vs distinct Roman epochs). Also note the effect is identical for the shipped and fine-tuned checkpoints, so the shipped-vs-fi | CONFIRMED | FIXED — same change as 29 (frac = distinct epochs / factor). |
| 29 | major | Y | `binml/preprocess.py:86` | bin_band computes each bin's mean/min/max over every raw observation that falls in the bin rather than over the (at most `factor`) Roman epochs the training cache pools, so the min/max channels inflate with the input sampling density and the documented equivalence with pipeline.cache.bin_curve holds only for exactly-on-grid input. *Verifier correction:* Downgraded from critical: I verified the binning inflation and the broken equivalence claim, but not the two downstream numbers. The claimed 0.563 -> 0.447 shift in P(NonPSPL) was not reproduced here, and the premise that RMDC26 samples the colour bands every ~24 min is asserted without evidence in  | CONFIRMED | FIXED — bin_band now reduces to one value per Roman epoch before binning (bit-identical on-grid: verified on 32 band-curves; matches cache.bin_curve off-grid). GULLS re-scored from cache: the effect was NOT negligible as first estimated — 1S1L false alarms at threshold 45.6%→35.6% (shipped) and 9.7%→11.7% (fine-tuned); planetary recall at threshold 0.36→0.46. Raw-observation pooling kept as transfer_full_*_rawpool.json; sensitivity disclosed in REVISION.md. |
| 18 | major | Y | `pipeline/evaluate.py:385` | `efficiency_plane` is called with the FULL 450,589-row pool instead of the test rows `ti`, contradicting metrics.json's own `_split_note` and the committed artifact, which was computed on the 360,472 test rows only. | CONFIRMED | FIXED — efficiency_plane on [ti]; regression test pins code to the committed (test-only) artifact. |
| 16 | major | Y | `pipeline/run_shard.py:195` | The out-of-range (OOR) shard mix is a dict literal with a duplicate key, so the swept class collapses from 9,000 events/shard to its context value (500-1,500) and never dominates the shard, contradicting the comment two lines above. *Verifier correction:* Severity major rather than critical: the OOR recall estimates are not biased by this, they are 9-18x noisier than designed and the shards are ~89% in-distribution filler. The reported recalls (e.g. widesep_np_recall 0.222, longp_per_recall 0.054, shortte_pspl_recall 0.524) rest on correspondingly sm | CONFIRMED | FIXED code (run_shard.py); paper now quotes swept-class n via new \bmlStress*N macros (from stress_report.json). |
| 10 | major | Y | `pipeline/train.py:200` | The 'nothing detectable survives -> Flat' relabel in `_apply_cadence` (and `_apply_gaps`) compares a NOISY max over the stored min/max channels against the 0.02 mag floor, and photometric noise alone exceeds that floor for essentially the whole population, so the branch is dead code. *Verifier correction:* Note the second relabelling branch in both functions (caustic bin dropped -> I_PSPL, lines 204-211 / 256-262) is unaffected and does work; only the 'nothing detectable survives -> Flat' branch is dead. Severity major rather than critical: it degrades the augmentation's labels, but no reported metric | CONFIRMED | OPEN (Phase 3) — dead Flat branch; noise-aware floor needed. |
| 9 | major | Y | `pipeline/train.py:208` | `_apply_cadence` and `_apply_gaps` test a 6-hour window derived from `t_anom`, but `t_anom` is quantised by assemble._anomaly_onset_day to only ten values (multiples of 7.2 d), so the window tested has no relation to where the caustic actually is. *Verifier correction:* One guard the finding does not mention: both branches are gated on `'t_anom' in pf_idx`, and pf_idx comes from the cache's meta.json param_fields. The shipped evaluation cache (paper/results/meta.json) lists only 15 fields with no t_anom, while writer.PARAM_FIELDS now has 16 — so on caches of that v | CONFIRMED | OPEN (Phase 3) — documented in REVISION.md and paper (7.2-d resolution caveat). Fixing changes --gap-aug semantics vs g08e12; do under a version flag. |
| 8 | major | Y | `pipeline/train.py:111` | The periodic branch of `_visible_amplitude` returns a quantity that is not an observed F146 magnitude amplitude, yet it is compared against the 0.02 mag detectability floor, so the truncation relabelling for PeriodicVar/LongPeriodVar is wrong by factors of 3-100 and in both directions. *Verifier correction:* The dominant error is not just the missing multiplicative factors: `amp*(1-cos(phase))` measures the RANGE TRAVERSED inside the revealed window, whereas the rule it is compared against (assemble.py:307/337, max\|mag_true - m_base_band\|) is DEVIATION FROM BASELINE. That is why the bias reverses sign | CONFIRMED | OPEN (Phase 3) — periodic amplitude proxy; training-label quality only; no reported metric. |
| 55 | major | Y | `validation/baselines.py:110` | The fitted-PSPL baseline is scored on an 800-point thinning while BinML and the feature baselines get all 6912 epochs, and the code's stated justification is wrong because the returned score is a fixed-width residual-window statistic, not a fit quality. *Verifier correction:* One part of the finding is wrong: paper.tex:627-630 does NOT repeat the false fairness claim — it discloses the thinning explicitly ('the five-parameter Paczynski fit uses a thinned 800-epoch subset for tractability, so its score is a computationally thinned estimate'). The manuscript's error is onl | CONFIRMED | FIXED + RERUN — full cadence: ap_fitted_pspl_residual 0.261 -> 0.545 (all other numbers seed-identical). canonical_numbers.json and MANIFEST.json updated; validate_artifacts green; paper prose updated. |
| 57 | major | Y | `validation/modal_ablations.py:109` | The still-cited cascade arm skips training on checkpoint-file EXISTENCE, but pipeline.train rewrites its output at every new best epoch, so an interrupted arm is silently reused while the result asserts epochs: 10. *Verifier correction:* Whether the reported result is actually corrupt is unverified and cannot be checked from the repo: ckpt_cascade_off.pt lives on a Modal volume, and this session has no budget or authorisation to fetch it. So the finding establishes an unclosed provenance hole in a reported ablation, not a demonstrat | CONFIRMED | VERIFIED NOT TRIGGERED — ckpt_cascade_off.pt epoch field = 9 (10/10). .done marker + per-arm trained-epoch provenance added. |
| 54 | major | Y | `validation/modal_cadence.py:81` | The cadence-comparison arms are evaluated on the same memmap they were trained on, so every reported metric (which reaches the paper as \bmlCadFifteen*/\bmlCadTwelve*) is computed on ~80% training data. *Verifier correction:* Severity major rather than critical, because the load-bearing claim survives: both arms share the identical defect and the identical seeds, so the 15-vs-12-minute CONTRAST the paper actually draws ('bounds the rate change as unimportant') is not biased. What is inflated is the absolute level of ever | CONFIRMED | RERUN IN PROGRESS (2026-09-09) — validation/cadence_local.py: same training shards/seeds/recipe as the Modal run, evaluated on disjoint shards 100-103; also scores the training pool to measure the inflation. Paper text currently carries the disclosure; will be replaced by the held-out numbers. |
| 31 | major |  | `binml/preprocess.py:55` | estimate_baseline returns the 90th percentile of the magnitudes, which for a non-varying source is biased FAINT by 1.2816*sigma; the resulting baseline-relative curve reads as a constant brightening well above the 20 mmag Flat/event amplitude floor, flipping Flat to PSPL or LongPeriodVar on the shipped default code path. *Verifier correction:* preprocess.py:132-136 does emit a warning when the baseline is estimated — but the warning names the opposite failure ('can misclassify (e.g. microlensing -> LongPeriodVar)'), and neither it nor the docstring at lines 47-52 discloses that the estimator manufactures a constant brightening of 1.28 sig | CONFIRMED | FIXED — `binml classify` requires --m-base unless --estimate-baseline; docstring and warning now state both bias directions. |
| 3 | major |  | `pipeline/photometry.py:96` | The CALIBRATION STATUS block's reasoning about the F087 saturation limit is backwards in both of its steps, and the resulting "audited" value saturation_ab=16.1 (line 129) is roughly 2.5 mag in the wrong direction; the same backwards claim is asserted in paper.tex:820-822, docs/pipeline.md:171, docs/model_card.md:33 and README.md:235. *Verifier correction:* One physical mitigation the finding omits: saturation is set by peak-pixel counts, and F087's narrower PSF concentrates ~ (1.46/0.87)^2 = 2.8x more flux in the central pixel, worth ~1.1 mag. Including that, an equal-well F087 limit is ~14.7 AB — essentially equal to F146's 14.8, still nowhere near 1 | CONFIRMED | FIXED — photometry.py comment + ROMAN_BANDS_AUDITED F087 saturation 16.1 -> 13.6 (equal-well, derived); paper.tex, README, model card, pipeline.md corrected. |
| 21 | major |  | `pipeline/plots.py:138` | The 'prevalence' chance-level line on the NonPSPL precision-recall panel is the UNWEIGHTED label fraction, while both PR curves on that axis are population-weighted; the reference is 2.13x too high. | CONFIRMED | FIXED — plots.py draws the weighted prevalence (0.056), matching the abstract. |
| 46 | minor | Y | `paper/REVISION.md:85` | The cross-simulator transfer table reports the shipped model's 1S2L NonPSPL argmax fraction as 0.77; the artifact value is 0.7647, which rounds to 0.76. | CONFIRMED | FIXED — REVISION.md 0.77 -> 0.76. |
| 74 | minor | Y | `paper/make_macros.py:12` | figures_stats.json is the only numeric input to make_macros with neither a manifest hash nor a trace-sha check, is gitignored, and is loaded fail-open (`else {}`), so a stale copy silently supplies 13 macros. *Verifier correction:* The 'fail-open' framing is wrong and should be dropped. A MISSING figures_stats.json is not silent: the `if _k not in fs: raise SystemExit('FATAL: ...')` guards at lines 324-327 and 372-376 abort make_macros.py before any of the permissive `if key in fs` blocks matter. Also paper/build.sh runs make_ | CONFIRMED | FIXED (fail-closed) |
| 73 | minor | Y | `paper/make_macros.py:207` | bmlFtWorstRegress is `abs(min(deltas))`, which reports the smallest *improvement* as a regression magnitude when no class regressed. | CONFIRMED | FIXED |
| 69 | minor | Y | `paper/make_macros.py:18` | make_macros has no NaN guard anywhere, so a NaN in any source artifact is emitted as the literal macro body `nan` instead of aborting, and every downstream check still passes. | PLAUSIBLE | FIXED (NaN guard) |
| 76 | minor | Y | `paper/paper.tex:936` | A count taken straight from cascade_matched_result.json is hand-typed into the prose ("disagree on 16 events, and all 16 are premature"), contradicting make_macros.py's docstring claim that every number in paper.tex is a macro and cannot drift. | CONFIRMED | FIXED (macros) |
| 75 | minor | Y | `paper/validate_artifacts.py:124` | Artifact validation is driven entirely by the manifest's own contents, so removing an entry silently exempts that artifact and the script still exits 0 announcing success; no test pins which artifacts must be covered. | CONFIRMED | noted |
| 14 | minor | Y | `pipeline/mix_finetune.py:61` | The module docstring asserts the natural pool is restricted to cache shards 0-89 so it 'cannot contaminate the test set', but the code globs every *.h5 in --nat-dir with no shard-index filter and no assertion. *Verifier correction:* No leakage is demonstrated — the finding is that the claimed invariant is unenforced and that meta.json would record it as satisfied even if it were not. A one-line assertion on the shard index parsed from each basename (or on len(nat) == 90) would make it checkable. | CONFIRMED | UNRESOLVED FROM REPO — natural pool dir contents not verifiable without AWS login; lineage notes + the 4.5M unseen-natural stress result argue against test contamination. Close by listing s3://.../v5/natural shard indices. |
| 65 | minor | Y | `validation/modal_gap_finetune.py:155` | keep_prob-reweighted precision/recall are reported next to raw unweighted support counts, so the printed n overstates the effective sample size for the classes that carry the reweighting. *Verifier correction:* Two details are off. The artifact's Flat recall is 0.975, not 0.960. And the 1520/1750 keep_prob composition is an assumption, not measured from the eval shards — the actual effective-n deflation for the Flat and PSPL rows in this run was not derived, so '25% too narrow' is illustrative rather than  | CONFIRMED | NOTED in result |
| 64 | minor | Y | `validation/modal_gap_finetune.py:131` | In the 'random' gap mode one gap pattern is drawn per 512-event batch, so the reported per-class numbers rest on ~30 gap draws while the support counts printed beside them imply thousands. *Verifier correction:* The finding's own caveat is the important one and should lead the write-up: both nets are scored against the same 30 patterns, so the shipped-vs-fine-tuned contrast — which is the claim REVISION.md actually makes — is paired and safe. Only the absolute 'random' column values and their implied precis | CONFIRMED | NOTED in result |
| 61 | minor | Y | `validation/modal_gap_finetune.py:137` | The 'RMDC26 seven-gap schedule' is described as an F146-only pause but is applied to F087 and F213 as well, and the coarse-grid `.any(1)` widens each 6.2 h gap into a full 18 h colour bin. *Verifier correction:* The consequence claim is much weaker than stated. validation/gulls/gap_sensitivity.py implements the same perturbation CORRECTLY — it drops observations by TIME from every band (lines 45-51), so a colour bin keeps its surviving epochs — and it gives shipped PSPL recall 0.11 and Flat 0.08 under the R | CONFIRMED | FIXED (wording) |
| 58 | minor | Y | `validation/modal_gap_finetune.py:87` | The fine-tuned checkpoint is cached on the free-form `tag` alone, while the recorded hyper-parameters are copied from the CLI, so a rerun under the same tag writes a result asserting hyper-parameters the checkpoint was never trained with. *Verifier correction:* Severity minor: the committed validation/gulls/gap_finetune_g08e12.json does record epochs 12, gap_aug 0.8, lr 1e-4 consistent with its tag, so no published number is currently mis-attributed. This is a reproducibility/provenance hole, not a wrong result. The seed-base half is also narrower than sta | CONFIRMED | FIXED (tag guard) |
| 59 | minor | Y | `validation/modal_gulls_transfer.py:44` | The GULLS/RMDC26 dataset is fetched from the mutable `/resolve/main/` ref with no revision pin, and neither the summary nor the staged artifacts record which revision was used. *Verifier correction:* The finding's core claim is false for the RELEASED result. validation/gulls_transfer.py — the script that produced validation/gulls/transfer_shipped.json — is pinned: line 65 sets REVISION = 'a338d5bab441b5caf551d2fea9469aadfdc81ec1', line 66 builds BASE from it, and line 405 writes it into the outp | CONFIRMED | FIXED (revision pinned) |
| 37 | minor |  | `binml/classifier.py:157` | predict_evolution's per-step guard only tests that some revealed F146 point is finite, not that any falls inside the window, so to_tokens raises mid-loop instead of leaving the NaN row the docstring promises. | CONFIRMED | FIXED |
| 42 | minor |  | `binml/legacy/cli.py:35` | `from . import __version__` inside main() raises ImportError because binml/legacy/__init__.py defines no __version__, so the entire legacy CLI is unreachable for every subcommand. *Verifier correction:* A crash, not a wrong number, and confined to the legacy CLI; severity minor for a paper audit but a real ship-blocker for the released package. | CONFIRMED | FIXED |
| 41 | minor |  | `binml/legacy/cli.py:105` | The `evolution` subcommand registers --flux via add_common but never forwards it to predict_evolution, so the flag is silently ignored on that path while `classify` honours it. | CONFIRMED | noted |
| 39 | minor |  | `binml/legacy/evaluate.py:89` | max(conf[i].sum(), 1) and max(conf[:, i].sum(), 1) turn an undefined recall/precision (a class with no true instances, or no predictions) into 0.0, which is indistinguishable from complete failure on that class and propagates as a real zero into any macro average. | CONFIRMED | noted |
| 38 | minor |  | `binml/legacy/evaluate.py:117` | A NaN anomaly_dchi2 fails `an_b >= thr`, so binaries whose delta-chi^2 could not be computed are silently counted as physically indistinguishable, inflating indistinguishable_frac and removing them from the recall_detectable denominator. *Verifier correction:* I could not establish that NaN anomaly_dchi2 actually occurs in any dataset this repo produces (the docstring points at a v1-era simulate.py that is not in the tree), so this is a latent robustness defect in the legacy package rather than a wrong number. Severity minor. | CONFIRMED | noted |
| 33 | minor |  | `binml/legacy/model.py:145` | The BN_MOMENTUM comment states the PyTorch convention backwards ('Higher values give MORE weight to running statistics, resulting in SLOWER adaptation ... We use 0.2 for more stable running statistics'); PyTorch uses running = (1-momentum)*running + momentum*batch, so 0.2 adapts twice as fast and gives LESS stable running statistics than the 0.1 default. *Verifier correction:* Severity is minor rather than major: this is the legacy 3-class module, no number in the paper depends on it, and the consequence is a hyperparameter chosen on backwards reasoning (roughly double the intended sampling variance in the eval-time running statistics), not a miscomputed quantity. | CONFIRMED | noted |
| 34 | minor |  | `binml/legacy/preprocess.py:106` | When the peak-centred window holds more than seq_len points the code keeps the EARLIEST seq_len and silently drops the tail, breaking the 'window centred on the peak' contract; the accompanying comment claims 'real curves never approach this', which is false for any cadence finer than 15 min. *Verifier correction:* Severity minor: legacy v1 code with no paper dependency, and the comment's claim ('real curves never approach this') is defensible for the ground-survey data this model targets — reaching 6,912 points inside a 72-day window needs >96 epochs/day, which no ground network delivers. The defect is that w | CONFIRMED | noted |
| 32 | minor |  | `binml/legacy/preprocess.py:117` | length=int(max(n,1)) converts a window containing ZERO observations into a length-1 all-zero sequence, so the legacy classifier returns a fully confident label instead of refusing to score. *Verifier correction:* Severity minor rather than major: this is the preserved legacy v1 3-class API (binml.legacy, exercised only by tests/test_smoke.py). No paper number, figure or macro depends on it. It is still a genuine user-facing defect in the released package — an empty window should raise rather than return a co | CONFIRMED | noted |
| 40 | minor |  | `binml/legacy/surveys.py:35` | load_moa decides whether column 1 holds magnitudes or difference flux with the heuristic np.nanmedian(c1) > 5, which misclassifies any difference-flux file whose median flux exceeds 5 as already-magnitudes and passes the raw fluxes through untouched. *Verifier correction:* The defensible statement is narrower: the magnitude-vs-flux discrimination is an undocumented heuristic on an arbitrary threshold, and the docstring should say so; the claimed failure mode (a difference-flux file with median 120) is not established as realistic. | PLAUSIBLE | noted |
| 36 | minor |  | `binml/preprocess.py:144` | The out-of-window warning tests (ta < t_start) \| (ta > t_start + WINDOW_DAYS), a different criterion from the epoch test that actually drops points, so it reports observations as dropped that were used and stays silent for observations that really were dropped. *Verifier correction:* Genuine but of very low consequence: the mismatch is confined to points within half an epoch (6.25 s at F146) of either window edge, and the warning is additionally gated at >5% of finite points, so it needs a tiny or edge-heavy input to surface at all. | CONFIRMED | noted |
| 35 | minor |  | `binml/preprocess.py:78` | Because epochs are obtained by round((t-t_start)/step), the accepted window is [t_start - step/2, t_start + 72 - step/2), not the [t_start + i*w, t_start + (i+1)*w) partition the bin_band docstring at lines 62-65 asserts; the last half-epoch is dropped and a half-epoch of pre-t_start data is used. | CONFIRMED | noted |
| 78 | minor |  | `paper/make_data_figures.py:263` | fig_latency's docstring says both figures now read validation/cascade_events.json; the function actually reads validation/cascade_trace.npz plus cascade_reproduce_result.json. *Verifier correction:* Slightly overstated on the 'unpinned input' half: lines 273-278 do hash-verify cascade_trace.npz against summ['reduction_provenance']['input_trace_sha256'] and abort on a mismatch, so the real input is pinned to its reducer even though it is misnamed in the prose. The finding is a stale docstring na | CONFIRMED | noted |
| 77 | minor |  | `paper/make_data_figures.py:287` | The premature and post-onset latency histograms share bin edges that do not include 0, and are drawn unstacked, so 5 of the 16 premature alerts sit in a bin dominated by 181 post-onset alerts and are invisible in the figure that exists to show them. | CONFIRMED | noted |
| 27 | minor |  | `pipeline/agg_stress.py:42` | Each per-class record stores population-weighted recall/precision/F1 next to an UNWEIGHTED support count `n`, with no field naming the difference, so the record cannot be recombined correctly. | CONFIRMED | noted |
| 6 | minor |  | `pipeline/assemble.py:292` | The per-band baseline is set from differential extinction only, while the per-band source fraction f_s_b (line 278) is shifted by the blend-source colour; no (source, blend) SED pair satisfies both, so the simulated two-component photometry does not conserve flux between bands and the baseline colour carries a systematic error. *Verifier correction:* Impact is a simulator-realism / internal-consistency issue, not a wrong number: the labels, the delta-chi^2 statistics and every reported metric are all computed self-consistently within this generative recipe. The exact colour-A_Ks degeneracy is largely a consequence of the already-disclosed 'sourc | CONFIRMED | noted |
| 7 | minor |  | `pipeline/baseline.py:107` | `L = BIN_FACTORS[REF] and 864` is a truthiness expression, not an arithmetic one: it evaluates to 864 for any non-zero bin factor, so the memmap row length is hard-coded and silently decouples from BIN_FACTORS. | CONFIRMED | FIXED |
| 4 | minor |  | `pipeline/baseline.py:100` | fit_event computes and stores a peak `amplitude` but the 0.02 mag amplitude floor that co-defines BOTH label boundaries is never applied anywhere, so the baseline is not "the same decision rule the LABELS were defined by" as the module docstring claims; the amplitude is also measured one-sidedly (brightest bin only), so it would be near zero for a dimming variable even if it were used. *Verifier correction:* Same scope caveat as id 2: this module does not produce any number in the paper (the baseline table comes from validation/baselines.py). The finding is a genuine code-vs-docstring mismatch in an internal diagnostic, not a wrong published number. | CONFIRMED | noted |
| 2 | minor |  | `pipeline/baseline.py:74` | The classical baseline re-estimates the flat level as an in-window weighted mean while the network is handed the analytic model baseline (cache.py:125 stores feat as mag - m_base_ref), so the docstring claim that this is "the FAIREST possible baseline" and that any margin is "from reading light-curve morphology... rather than from having better access to the labelling rule" is false. *Verifier correction:* The defect and mechanism are real, but the severity is lower than stated: pipeline/baseline.py + pipeline/report.py do NOT feed any published number. paper/canonical_numbers.json['baselines'] and Table~\ref{tab:baselines} come from validation/baselines.py -> baselines_result.json, and no 'classical  | CONFIRMED | noted |
| 25 | minor |  | `pipeline/evaluate.py:325` | Under --max-events, `label`, `true_class` and `keep_prob` are never subset inside `cols`, so the saved artifact mixes full-length columns with subset-length logits. | CONFIRMED | FIXED |
| 22 | minor |  | `pipeline/evaluate.py:149` | macro-F1 drops any class the model never predicts (precision is None) or that scores rec+pre==0, instead of scoring it 0.0, so a total failure on a class silently raises macro-F1; the docstring claims the exclusion applies only to zero-SUPPORT classes. *Verifier correction:* Severity minor rather than major: no published number is affected. _prf is used only inside evaluate_checkpoint (grep confirms no other caller), the committed metrics.json has all six classes present and predicted, and paper/results/stress_report.json's per-regime macro-F1 is computed independently  | CONFIRMED | FIXED |
| 17 | minor |  | `pipeline/evaluate.py:379` | `false_anomaly_by_true_class` buckets by the observational LABEL `y`, not by `true_class` as its name states, so ~74% of the reported 'false anomaly' calls are genuine binaries whose anomaly fell below the detectability cut. *Verifier correction:* The computation is not wrong, only the key name. Everything else in evaluate.py scores against the observational label, under which a demoted binary called NonPSPL genuinely IS an error the model is charged for — so the by-label breakdown is the right quantity and the number is correct for what it c | CONFIRMED | FIXED (bucket by true class; artifact key regenerates at next evaluate run) |
| 5 | minor |  | `pipeline/generators.py:383` | LongPeriodVarGen._shape normalises on a reference grid of 4x the LONGEST period, which for the multi-mode OSARGs (60% of the class) covers only ~20% of a beat cycle, so ptp on the reference underestimates the true intrinsic peak-to-peak and the delivered amplitude exceeds the drawn amp_I; the docstring one line above claims the grid spans "several beat cycles". *Verifier correction:* Two overstatements. The maximum ratio I measured is 2.76x, not 3.48x (and the finding's own worked example gives 1.95x, not 3.5x). More importantly, what reaches the light curve is the IN-WINDOW shape: over 3,000 OSARGs the 72-day ptp of _shape has median 0.919 and exceeds 1 for only 32% of objects  | CONFIRMED | noted |
| 23 | minor |  | `pipeline/pilot.py:102` | The Flat-label invariant in the pre-production validation gate is `check(<condition> or True, "")`, which can never fail and would print an empty message if it could. *Verifier correction:* The disarming looks deliberate rather than accidental — the trailing comment '# amplitude floor also applies' shows the author knew the naive assertion is false (a Flat label can also arise from max_amp < 0.02 with dchi2_event >= 500). The real defect is that a check that cannot be written correctly | CONFIRMED | FIXED |
| 28 | minor |  | `pipeline/plots.py:130` | The classical-baseline branch builds its population weights in float32, reintroducing the float32-cumsum overshoot that line 57 and evaluate.py:134-138 explicitly cast to float64 to avoid. *Verifier correction:* The finding's own caveat is right and worth keeping in the write-up: every NonPSPL row has keep_prob exactly 1.0 (I checked: 100% of y==NonPSPL rows), so the recall numerator is an exact integer cumsum and br cannot exceed 1 today; only the baseline precision curve is perturbed at ~7e-4, in the 4th  | CONFIRMED | noted |
| 24 | minor |  | `pipeline/plots.py:454` | Every figure in make_all is computed over the full 450,589-row pool; test_idx.npy is loaded in only two figures and only as a light-curve row pointer, so published diagnostic figures include the 90,117 validation rows the frozen threshold was selected on. *Verifier correction:* Scope: pipeline/plots.py is the internal diagnostic set only. paper/make_figures.py:44-49 explicitly restricts every paper figure to test_idx.npy, so no published figure is affected. | CONFIRMED | noted |
| 20 | minor |  | `pipeline/plots.py:402` | fig_efficiency_planes still computes panel C as B/A clipped at 1.5 and captions it '>1 = flags sub-threshold binaries', which is the exact quantity evaluate.py and paper/make_figures.py both document as an error with mismatched bases. *Verifier correction:* Severity minor, not major: pipeline/plots.py produces internal diagnostic PNGs (08_efficiency_planes.png) only. Every figure in paper.tex comes from paper/outputs/figures/*.pdf, generated by paper/make_figures.py, whose fig_efficiency_plane uses the corrected bounded conditional recall. No published | CONFIRMED | noted |
| 19 | minor |  | `pipeline/report.py:91` | report.figures() asks for the plane key `classifier_efficiency`, which `evaluate.efficiency_plane` no longer emits (it returns `classifier_recall_given_detectable`), and its panel title still asserts the B/A definition that evaluate.py documents as wrong. *Verifier correction:* The primary consequence is a crash, not a wrong number: pipeline/report.py cannot complete on any cache with params. Severity is lower than 'major' on this codebase's own weighting, though it does mean the module is a dead reproduction entry point. | CONFIRMED | noted |
| 26 | minor |  | `pipeline/run_shard.py:219` | The regime name is not part of the shard seed (`seed = seed_base + shard*7919`), so regimes sharing a --seed-base and a class mix draw an identical RNG stream and produce the same events with one parameter swapped. *Verifier correction:* One extra check worth noting while you are in this code: oor_pspl_lowfs sets params['f_s'], which no generator or assembler ever reads (assemble.py samples f_s_ref at line 235, before param_override, and blending uses only f_s_ref). That regime therefore sweeps nothing — it is an in-distribution PSP | CONFIRMED | noted |
| 11 | minor |  | `pipeline/train.py:248` | `_apply_gaps` maps the F146 blank mask onto the colour grid with `.any(axis=1)`, which blanks a whole 18-hour colour bin whenever any 2-hour reference bin inside it is blanked, removing 3.4x more colour coverage than the gap actually spans. | CONFIRMED | noted |
| 15 | minor |  | `pipeline/train.py:628` | The best-model checkpoint records config, mag_scale, epoch, val F1 and seed, but not --label-source, --weight-labels, --alpha-nonpspl, --truncate-aug, --cadence-aug, --gap-aug or the cache path, so the paper's ablation arms produce checkpoints that are indistinguishable by their recorded metadata. | PLAUSIBLE | noted |
| 79 | minor |  | `pipeline/train.py:243` | The gap start is drawn from rng.integers(0, nb - width), whose upper bound is exclusive, so no gap can ever extend to the final F146 bin; tests/test_gap_augmentation.py does not check this. | UNVERIFIED | noted |
| 13 | minor |  | `pipeline/writer.py:75` | `_param_vector` stores `max(period)` for LongPeriodVar while the comment on line 71 says it records 'the dominant period', biasing the stored P high for the 60% of LPVs that are multi-periodic OSARGs. | CONFIRMED | FIXED (comment) |
| 72 | minor |  | `tests/test_gap_augmentation.py:46` | test_flat_stays_flat is trivially true: _apply_gaps returns the label immediately for I_FLAT before any relabelling logic runs, so the assertion holds for every possible implementation of the rules the file exists to pin. *Verifier correction:* Narrower than stated: the sibling test test_gaps_are_contiguous_and_consistent_across_bands does pin the blanking contract (contiguity, band consistency, zeroed channels) for a non-Flat label, so only the Flat-specific blanking and the relabelling branches are uncovered. This is a test-quality gap,  | CONFIRMED | FIXED (Flat test now checks blanking) |
| 71 | minor |  | `tests/test_gap_augmentation.py:32` | The gap-extent bound `m146.sum() <= 8 * 12 * 12` evaluates to 1152, which exceeds the 864-bin F146 array, so the assertion can never fail; the comment's own arithmetic gives 48. | CONFIRMED | FIXED (bound 48) |
| 70 | minor |  | `tests/test_validation_and_invariants.py:78` | test_efficiency_map_is_a_bounded_probability's only real assertion is a tautology: it computes one global mean of booleans and asserts it lies in [0,1], which no implementation can violate, and it never touches the per-(q,s)-cell quantity its docstring names. *Verifier correction:* The stated consequence is wrong, and this materially lowers the finding's value. Reintroducing the B/A bug into pipeline.evaluate.efficiency_plane would NOT slip through: the immediately following test_evaluate_efficiency_function_is_bounded (lines 81-101) calls efficiency_plane directly on syntheti | CONFIRMED | FIXED (bound vs headline) |
| 49 | minor |  | `validation/cascade_matched_reduce.py:222` | `detection_ci` is a Wilson binomial interval computed on `achieved`, a detection count that the matching rule fixes by construction rather than estimates from data. | CONFIRMED | noted |
| 48 | minor |  | `validation/cascade_matched_reduce.py:235` | The sign of `delta_premature_on_minus_off` and the a/b labelling of the McNemar counts are taken from arm insertion order (`a, b = list(row)`) with no check that the first arm is `cascade_on`. | CONFIRMED | DEFERRED — reducer is sha-pinned by the manifest; make a/b explicit at the next regeneration |
| 45 | minor |  | `validation/cascade_matched_reduce.py:5` | The WHY paragraph motivates the matched-detection experiment with premature rates (0.123 vs 0.193) taken from a different 300-event artifact scored on the COARSE 7.2 d onset, while this script reduces a 400-event trace on the FINE 0.5 d onset where the same comparison is 0.010 vs 0.170. *Verifier correction:* The defect is narrower than stated: the docstring's numbers are correct for the artifact they describe. What is missing is any note that the motivating comparison used a different onset definition (coarse 7.2 d) and a different event set (300 vs 400), so a reader placing 0.123 next to this script's  | PLAUSIBLE | noted |
| 47 | minor |  | `validation/gulls/gap_sensitivity.py:11` | The docstring's RMDC26-schedule recalls (PSPL 0.12, -> NonPSPL 0.65, PeriodicVar 0.18) disagree with the committed gap_sensitivity.json produced by this script (0.11, -> 0.69, 0.16). | CONFIRMED | FIXED (docstring) |
| 44 | minor |  | `validation/gulls/gap_sensitivity.py:16` | The docstring names `pipeline/train.py --cadence-aug` as 'the designed remedy' for the gap failure; the actual remedy is `--gap-aug`, and paper/REVISION.md states explicitly that --cadence-aug addresses a different regime. *Verifier correction:* Severity minor rather than major: this is one stale sentence in a diagnostic script's docstring. The two authoritative documents a reader would follow (REVISION.md and gulls_transfer.py) both name --gap-aug correctly, and validation/modal_gap_finetune.py:51 actually passes --gap-aug. | CONFIRMED | FIXED (docstring) |
| 52 | minor |  | `validation/gulls/transfer_reduce.py:46` | rate() returns {"n": 0} with no "rate"/"ci95" key for an empty selection, so slice_curve emits response-curve cells that lack the fields a consumer reads; two such cells exist for the committed rows. *Verifier correction:* No published number is wrong: there is no committed transfer_reduced.json and no consumer of these curves in paper/ or make_figures.py, so this is a schema inconsistency waiting for its first reader rather than a live defect. | CONFIRMED | noted |
| 50 | minor |  | `validation/gulls_transfer.py:388` | P(NonPSPL) is persisted rounded to 4 decimals and then compared against the 16-digit operating threshold 0.9042405486106873, both here (line 429) and in transfer_reduce.py (lines 103-104, 132) - a 1e-4 quantisation of the exact quantity this repo elsewhere records float16 (5e-4) as corrupting at this same threshold. *Verifier correction:* The magnitude is overstated by an order of magnitude. Rounding to 4 dp only changes the decision for true probabilities in [0.90424055, 0.90425) — a window 9.4e-6 wide, not 1e-4 — because 0.9042 rounds below the threshold and 0.9043 above it. So the expected number of flipped events in a 1,473-event | CONFIRMED | NOTED — rounded P vs 16-digit threshold; both summary and reducer share the convention |
| 43 | minor |  | `validation/gulls_transfer.py:5` | The module's RESULT header states the shipped model returns PSPL 4% / NonPSPL 65% / PeriodicVar 31% on RMDC26 1S1L, but the artifact this script actually produced gives 2.9% / 68.8% / 27.2%, and no committed run reproduces 4/65/31. *Verifier correction:* Downgraded from major: the manuscript is not affected. paper/REVISION.md:84 quotes the 1S1L row correctly as 0.03 -> 0.66 / 0.69 -> 0.31 / 0.27 -> 0.00, matching the artifact. The stale 4/65/31 lives only in two module docstrings, so the risk is to a reader of the code, not to a published number. | CONFIRMED | FIXED — header cites the full-population artifact. |
| 51 | minor |  | `validation/gulls_transfer.py:374` | On a --curve-cache hit any event id missing from the cached npz is silently skipped with no row emitted, and nothing asserts n_classified + n_skipped == n_requested; the cache filename encodes only (first id, last id, count) and none of the parameters that determine block membership. | PLAUSIBLE | FIXED — cache hit requires every requested id; mismatches re-query; missing ids emit an explicit skipped row. |
| 53 | minor |  | `validation/inference_benchmark.py:66` | The comment describes the synthetic inputs as '5 channels per bin (mean/min/max magnitude, observed fraction, observed mask)' but the arrays built are 3-channel feats plus a separate frac vector, and the docstring's promise that 'Tokenisation costs are reported separately' is never fulfilled - the emitted JSON has no tokenisation timing at all. *Verifier correction:* The per-batch assembly inside the timed region makes the reported 359 events/s CONSERVATIVE (it understates throughput), which is consistent with the docstring's own stated intent, so no published throughput number is inflated. The defect is documentation: a promised separate tokenisation figure tha | CONFIRMED | noted |
| 60 | minor |  | `validation/kmtnet_validate.py:61` | Photometry from KMTNet's three sites is concatenated with no per-site zero-point alignment, which injects a ~1-day quasi-periodic systematic that plausibly causes the PeriodicVar verdict the script attributes to sampling gaps. *Verifier correction:* The defensible statement is that this particular script does not isolate the mechanism it attributes the result to, and adding per-site median subtraction would cost two lines and remove the ambiguity — not that the conclusion is wrong. No paper number depends on it (paper.tex:729 explicitly says 'E | PLAUSIBLE | noted |
| 68 | minor |  | `validation/microlia_compare.py:35` | MicroLIA is trained on a 400-day timestamp template from a single event but then asked to classify the full multi-year OGLE curves, and its features are baseline-length and cadence dependent. *Verifier correction:* The script's headline conclusion is unaffected: 'MicroLIA has no binary/anomaly class' is structural. Only the '~6/8 top-classified as microlensing' number is compromised, and it is a lower bound of unknown size. No paper macro or table cites it (grep finds no MicroLIA reference in paper.tex or cano | CONFIRMED | noted |
| 66 | minor |  | `validation/modal_hardregime_finetune.py:156` | All eleven hard training regimes are generated from the same two RNG seeds, so the seven pure-prior regimes reduce to two independent event draws rather than fourteen shards' worth. *Verifier correction:* Two corrections to the counting. (1) 'faint' also leaves SHARD_MIX and the priors untouched, so it shares the same stream and differs only in m_base_ref (24.44 vs 23.13 for event 0) — six regimes, not five, draw from one stream. (2) 'longte' and 'shortte' do NOT stay aligned: priors.sample_tE uses r | CONFIRMED | noted |
| 62 | minor |  | `validation/modal_labelling_ablation.py:268` | The docstring promises a manifest containing the git commit and dirty flag; neither is computed by the code nor present in the committed artifact. | CONFIRMED | noted |
| 63 | minor |  | `validation/modal_latency_gaps.py:90` | The 'matched visit count' gap test silently unmatches the two arms when the nightly pool is smaller than n_visits, reintroducing the visit-count confound the docstring says it eliminates. *Verifier correction:* No published number is affected. I simulated 120 detectable NonPSPL events and every one had all 6912 F146 epochs usable with a nightly pool of exactly 2375, so at the committed visits=1200 (latency_gaps_result.json records visits: 1200) both arms genuinely draw 1200 points and the match holds. The  | CONFIRMED | noted |
| 56 | minor |  | `validation/modal_latency_gaps.py:109` | The 'censor-aware' latency statistics are computed only over detected events, which is exactly the survivor bias the module docstring says it removes, and no Kaplan-Meier estimator is produced despite being promised. *Verifier correction:* Severity minor, not major: no paper number comes from this file. canonical_numbers.json['_latency_note'] states explicitly that latency numbers come from validation/cascade_reproduce_result.json via make_macros.py, and paper.tex:453-454 discloses that 'the latency statistic is conditional on an aler | CONFIRMED | noted |
| 67 | minor |  | `validation/real_data_validate.py:48` | The 72-day analysis window is centred on the single brightest point of a multi-year OGLE curve with no outlier rejection or magnitude sanity cut, so one spurious bright measurement silently windows out the real event. | PLAUSIBLE | noted |

## Refuted

- [1] `pipeline/assemble.py:50` — The SurveyConfig comment (and paper.tex:243-244) asserts that at Roman's sampling "BOTH chi^2 cuts are effectively vestigial, and it is the floor that actually  — *I re-derived both halves and the finding inverts the truth. (a) The '5.3%' is a weighted/unweighted mixing error. paper/results/keep_prob.npy shows the 78,157 demoted rows all carry keep_prob=0.15 (BY*
- [12] `pipeline/cache.py:125` — `arr = arr - m_base[:, None, None]` subtracts the F146 baseline from every band, but assemble.py gives each band its own baseline, so the F087/F213 channels car — *The behaviour is deliberate, documented, and correct, and neither claimed consequence holds. binml/preprocess.py:80 ('ONE reference baseline for all bands (colour survives)') and to_tokens' docstring *

## Author-side findings (outside the workflow)

- REVISION.md attributed the fine-tune's clean-data cost 'mostly' to PeriodicVar precision; per-class F1 deltas are Eruptive −0.025, NonPSPL −0.021, PeriodicVar −0.019. Fixed.
- REVISION.md printed 1S2L shipped argmax NonPSPL as 0.77; artifact 0.7647 → 0.76. Fixed.
- gulls_transfer.py --curve-cache key does not encode the selection; a mismatched hit silently drops events. Phase 2.
- Flat recall vs single-gap length is non-monotonic (1.00, 0.60, 0.81 at 2/4/6 h) on a matched event set; stated in REVISION.md and gap_sensitivity.py.

## Verified correct (independent re-derivation)

All macro-F1 rows of the gap fine-tune table; every gap-sensitivity cell; PSPL 0.078→0.885 and Flat 0.071→0.975; every transfer-table column and n = 279+527+480 = 1,286;
'0 training events with an empty F146 bin' (0/100 on a fresh sample); efficiency-plane artifact = test-only computation; ckpt_cascade_off epoch = 9.
