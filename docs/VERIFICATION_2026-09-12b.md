# Third verification of the revision work (2026-09-12/13)

Seven independent verifiers (referee-round recomputation, seed replicates and the seed rule, truth relabelling,
a referee's read of the whole paper, the macro generator, docs and ledger, reproducibility and CI) checked
everything added after the second pass (commits f2b4a21..c4bf5f3); each slice's findings were re-run by an
adversarial checker, and a completeness critic looked for what nobody had covered. Raw record:
`docs/verification/2026-09-12b_findings.json`; dispositions: `docs/verification/dispositions_2026_09_12b.py`
(this file is generated by `docs/verification/build_doc_2026_09_12b.py`, which asserts that every finding is
mapped to exactly one issue and that each issue's severity is its worst finding's).

**98 findings: 11 critical, 34 major, 53 minor; adversarial verdicts 80 real, 17 partly, 1 refuted.** They group into 55 issues.

## What changed as a result

* **The seed rule was misapplied.** The released run's rate-weighted lead over round 3 does exceed the weighted
  seed range, and the extra-training and physics verdicts flip with the budget. The three seeds are now a rough
  scale applied per budget and weighting (Table tab:seeds): the measured pauses lead everywhere; the extra
  training carries false alarms and the low budgets, the physics the high budget, mean recall and the largest
  rho/|u0| bins (per event only); the recommended recipe's lead over round 3 is not established.
* **The truth-label measurements were re-referenced** to the generator's own rule (a single-lens refit on the
  surviving epochs of each binary's rebuilt noise-free curve). The first reference was matched by construction.
* **Referee round:** the floor arms are partly the same events; the colour effect is lower variable-class
  precision; the colour fine-tune contrast depended on the kept epoch; the every-class scan is also reported with
  F146 alone, where purity is much lower; the per-event inputs are archived.
* **Two errors older than the revision (critic):** the 14.9M-event stress suite was scored with the stage-5
  checkpoint, not the released model; a regenerated subset scored with both now gives the released model's numbers
  (Table tab:stress), and shows that the suite's sub-day 'PSPL recall' mixed in demoted binaries: most sub-day
  single lenses are called anomalies. The appendix training recipe described the base run's defaults; it now
  gives the six-stage chain.
* **Paper:** a stale Table 5 note, the Introduction, the Conclusion's account of open tests, and a dozen smaller
  wording and labelling fixes; every new directional sentence has a fail-closed guard, and the guards are tested.

## Issues

| id | severity | issue | disposition | findings |
|---|---|---|---|---|
| I01 | critical | Seed rule: the released run's weighted lead (0.024) exceeds the weighted seed range (0.022), so 'does not pass' was false; the verdicts flip with the budget; a three-run range is a weak yardstick | Rewritten: the range is a rough scale, applied per budget and weighting in a new table (Table tab:seeds, bold where a difference exceeds it); verdicts restated from it and each guarded in make_gulls_macros.py; REVISION row 20 and CHANGELOG corrected. | seeds-and-rule-1, macros-generator-1, docs-consistency-1, repro-ci-1, seeds-and-rule-3, seeds-and-rule-7 |
| I02 | critical | Physics attribution holds per simulated event only; 'removed most of the remaining false alarms' was 45%; 'most of the gain is the extra training' false in mean recall | Attribution rewritten by budget and weighting (extra training: false alarms and low budgets; physics: 11.7%, mean recall, the two largest-ratio bins, per event; nothing weighted, n_eff ~100 and ~50); 'accounts for most of the further reduction'; REVISION row 13 qualified. | paper-referee-read-2, seeds-and-rule-4, macros-generator-8, paper-referee-read-3, docs-consistency-2, docs-consistency-16 |
| I03 | major | 'Its RMDC26 numbers are the best of the three seeds' true only for planetary recall | Narrowed to planetary recall at every table budget and at its own threshold, with seed 2's lower frozen-threshold false alarms stated; guard extended; README, model card, CHANGELOG, REVISION row 20. | seeds-and-rule-2, macros-generator-4, paper-referee-read-13, docs-consistency-8 |
| I04 | major | Frozen-threshold excess 'by less than its own seeds differ' read per bin fails in the 0.3-1 bin; 'every bin' unguarded | Worded as the overall rate against the seed spread (4.7-7.3%); 'every bin' guarded per event. | seeds-and-rule-5, macros-generator-5, docs-consistency-7 |
| I05 | major | 'Between two seeds they exclude zero' is false for seed 3 vs seed 2 | 'Can exclude zero', with the s3-s2 pair (includes zero) added to --pairs and quoted. | seeds-and-rule-6 |
| I06 | major | Single-run held-out AP differences (0.010, 0.019) interpreted though the seeds span 0.018 | Held-out comparisons state the seed spread: seeds 'tie or exceed' fspl5s with pauses and sit at or just below it clean; the Sec. limits pauses-vs-random-gaps AP comparison qualified. | seeds-and-rule-9, macros-generator-14, paper-referee-read-12 |
| I07 | minor | Budget slack in the Table 7 caption covered unweighted columns only (shipped weighted 5.2% budget is 0.09 points off) | Weighted slack computed and stated. | seeds-and-rule-10, macros-generator-13, docs-consistency-15 |
| I08 | minor | Seed provenance: 'same code and data' ignores the seed-dependent split and kept epoch; s3 trained at 1c4234f | PROVENANCE, REVISION row 20 and the generator comment now say same pool and recipe, the seed also sets the split and kept epoch (9/5/11), code differs only in comments and truth-gated branches. | seeds-and-rule-8, docs-consistency-9 |
| I09 | critical | LPV truncation 16.4% was a double rounding (408/2,496 = 16.3%) | truth_relabel_impact.py stores counts; macros computed once from counts; 16.3% everywhere. | macros-generator-2, docs-consistency-4, truth-relabel-3 |
| I10 | critical | Pause-relabel 10.5% counted both directions, and the 'label rule' was the full-season-fit approximation | Remeasured against the generator's own rule (refit on the surviving epochs): 9.4% given PSPL although the anomaly survives (relabel on), 2.5% kept NonPSPL unsupported (off); direction guarded. | macros-generator-3, paper-referee-read-15, docs-consistency-6, truth-relabel-2 |
| I11 | critical | 'The half-day onset reproduces the rule' was true by construction (the reference was the onset itself) | Reference replaced by a single-lens refit at the cut on each binary's rebuilt noise-free curve (reproduces all 858 stored labels; 5 unstable); fixed rule: 2.4% with the half-day onset, 6.4% with 7.2-d, the non-monotone cases; paper and train.py comments say so. | truth-relabel-1, macros-generator-10 |
| I12 | major | 'More than half' late PSPL depended on the reference | About half (49% against the refit reference); guarded as 40-60%. | truth-relabel-4 |
| I13 | major | Mixing truth and no-truth caches in to_memmap silently wrote zero truth rows that truth relabelling turns into Flat | convert and convert_selected refuse the mix (tests/test_truth_relabel.py). | truth-relabel-5 |
| I14 | major | AUDIT finding 9 contradicted itself; the truth-bin rule for heavy thinning is ~10% off | Finding 9 rewritten with the refit-reference numbers per augmentation (cadence: legacy 41%, truth bins 10%); train.py docstring no longer calls the approximation adequate for thinning. | truth-relabel-6 |
| I15 | minor | Finding 10: single-lens truncation errors are mostly event-chi2 failures, not amplitude | Reworded with the split (18 of 344 fail the amplitude floor). | truth-relabel-7 |
| I16 | minor | Truth bins: floor(t/72*864) sent 279 of 6,912 epochs one bin early | Binning by a tolerant floor in a tested helper (assemble._ref_bin = epoch k -> k // 8); truth shard regenerated; impact rerun. | truth-relabel-8 |
| I17 | minor | The LM refit is chaotic for rare events; part of the non-monotone onset is numerical | Disclosed in the paper (Sec. cascade) and PROVENANCE with measured rates (5 of 858 full-window binary decisions, 0.5% of truncations); the refit is not changed (it would change released labels). | truth-relabel-9 |
| I18 | minor | Self-consistency checks started from the stored label and could not catch a rule that fails to demote | Two-sided in the script and the test (from the generator class; every stored label reproduced). | truth-relabel-10 |
| I19 | minor | A truncation test exercised only the no-onset fallback | Commented as the fallback test; the onset rule has its own test. | truth-relabel-11 |
| I20 | minor | --truth-bins without --onset-resolution-days 0.5 silently kept the 7.2-d onset | run_shard warns; train warns when the cache's onset grid is coarser than half a day. | truth-relabel-12 |
| I21 | minor | Truth relabelling hard-coded the 0.02-mag floor | train refuses a truth cache generated at another floor. | truth-relabel-13 |
| I22 | minor | Artifacts stamped with a dirty tree before the code they depend on; unused imports; tables not compared in the test | truth_relabel_impact.json regenerated at a clean commit; referee_round.json re-reduced at one from cached intermediates made at earlier commits (PROVENANCE lists them; fourth verification); imports removed; the complete-inputs test compares every output. | truth-relabel-14, macros-generator-16 |
| I23 | minor | AUDIT ledger still listed findings 8-10, figures and Zenodo as open; 35.6 -> 11.7% | Ledger updated (decided and done; 11.6%). | truth-relabel-15, docs-consistency-12 |
| I24 | critical | The floor arms are partly the same events (21.6% / 4.5% shared), not independent draws | 'Partly overlapping draws' with the measured overlap (floor_overlap in the artifact); runner docstring, CHANGELOG, REVISION section 2. | referee-recompute-1 |
| I25 | major | The colour effect is lower variable-class precision (flat called periodic, single lenses called eruptive), not worse rejection of variables | Paragraph rewritten with precision and recall; directions guarded (precision down, recall unchanged, no more variables sent to microlensing). | referee-recompute-2 |
| I26 | major | The colour fine-tune contrast rested on single runs and the kept epoch; it vanishes one epoch later | Final-epoch evaluations added; the claim now rests on the audited fine-tune's drop on the old photometry (kept and last epoch) and states that the audited-photometry difference disappears at the last epoch. | referee-recompute-3, paper-referee-read-7, macros-generator-11 |
| I27 | major | Abstract and Conclusion called the three-band every-class scan 'the same way' and quoted the test set's prevalence | Scan run also with F146 alone; abstract and Conclusion name the bands, the simulated prevalence and 'detectable anomalies', and give the F146-only purity. | referee-recompute-4, paper-referee-read-4, docs-consistency-5, macros-generator-12, referee-recompute-9 |
| I28 | major | In-house 0.7% was the raw fraction; the RMDC26 comparison mixed checkpoint, bands, weighting and units | Reweighted and raw both given, generated single lenses and demoted binaries split; the RMDC26 comparison is now like for like (recommended checkpoint, F146, frozen threshold on our generated single lenses); RMDC26 figures labelled per event / weighted, per 1,000 single lenses per day. | referee-recompute-5, paper-referee-read-6 |
| I29 | major | Referee-round per-event inputs not distributed; Data availability silent on the new artifacts | Per-event inputs archived with hashes (validation/referee_round_archive/, --archive); Data availability names referee_round.json and truth_relabel_impact.json; dirty-tree fine-tunes and MPS non-determinism disclosed. | referee-recompute-6, paper-referee-read-11 |
| I30 | major | AP and F1 were on the evaluator's 80% split while completeness and purity used every event | All four on every event (all_events block); the evaluator's split values kept alongside. | macros-generator-6, referee-recompute-7, docs-consistency-14 |
| I31 | major | 'Test shards' 90-91 include ~15% of the rows the frozen threshold was chosen on | Called shards of the held-out pool, with the measured share (threshold_selection_overlap). | paper-referee-read-5, referee-recompute-8 |
| I32 | major | Docs and the fig. cascade caption still said no alert-burden or colour measurement exists | README, model card, usage, evaluation, pipeline docs and the caption point to the every-class scan and the colour result. | referee-recompute-10, docs-consistency-3, paper-referee-read-10 |
| I33 | minor | Colour fine-tunes: best epoch kept, not three epochs; learning rates | Described as up to three epochs keeping the best; the training-recipe paragraph gives the learning rates. | referee-recompute-11, repro-ci-2 |
| I34 | minor | Prior-shift purity also holds the non-anomalous mix fixed | Stated. | referee-recompute-12 |
| I35 | critical | Table 5 note still said the Paczynski fit used a thinned 800-epoch subset | Corrected: all methods at full cadence. | paper-referee-read-1 |
| I36 | major | The Introduction did not mention the RMDC26 test | A fourth contribution added. | paper-referee-read-8 |
| I37 | major | Conclusion: 'raised in review' and 'three tests' inaccurate; streaming calibration dropped from the open tests | Rewritten against the submitted version's list; a disjoint-prefix streaming threshold restored to the remaining tests. | paper-referee-read-9, docs-consistency-13 |
| I38 | minor | 'Delays, never advances' contradicted by the stored-truth check | 'Almost always delays'. | paper-referee-read-14 |
| I39 | minor | Abstract length and unlabelled weightings | 'Per simulated event' and 'of the planetary events we checked' added; the abstract is ~360 words against ~265 submitted (A&C's limit could not be checked offline; flagged to the author). | paper-referee-read-16 |
| I40 | minor | Detection comparison mixed checkpoint and simulator | Said so. | paper-referee-read-17 |
| I41 | minor | 'No operating threshold was chosen on it' while matched-budget thresholds are set on RMDC26 | 'No deployed operating threshold'; the reporting thresholds are named. | paper-referee-read-18 |
| I42 | minor | The list of schedule differences omitted the within-season pauses | Added. | paper-referee-read-19 |
| I43 | minor | Audited F087 saturation used at the 46.8-s exposure | Refuted: the audited limit is relative to F146 at equal exposure; no change. | paper-referee-read-20 |
| I44 | minor | Unlabelled Wilson intervals; 'vestigial' chi2 cuts vs RMDC26's 26% failing the cut | Intervals labelled; 'vestigial for our error model', and the RMDC26 labels section says the cut binds with GULLS' errors. | paper-referee-read-21 |
| I45 | minor | Stray space in corporate-author citations; per-event bins unlabelled; '1000' vs '1,000' | build.sh strips the bst's leading space; caption says per simulated event; count macros use separators. | paper-referee-read-22 |
| I46 | major | Fail-closed tests did not exercise the new guards | Value-perturbation cases added (floor direction, stream variable alert, colour recall, seed order, weighted physics, truncation late share, missing seed pair and refit reference). | macros-generator-7 |
| I47 | minor | --list-inputs omitted the two directly read JSONs | Both read through load(); a test asserts every listed input is hashed in the manifest. | macros-generator-9 |
| I48 | minor | Directional sentences without guards | Guards added for the ones this round touched (every bin, 2S2L 'by more', abstract 'raises', 'barely moves', 'largely intact', pause-relabel directions, colour directions). | macros-generator-15 |
| I49 | minor | REVISION housekeeping (mean 0.538, 'seventeen rows', row 19 file name, Zenodo) | Fixed. | docs-consistency-10 |
| I50 | minor | VERIFICATION_2026-09-12 H23 and notebook 01's 'a fifth of the selected events' stale | Notebook sentence corrected; this record supersedes H23 (the expansion is from the dataset card). | docs-consistency-11 |
| I51 | minor | Committed rendered draft stale | Regenerated; CI now compares regenerated macros, tables and rendered draft with the committed files. | repro-ci-3 |
| I52 | minor | The documented --scores clone command did not run | The artifact records command_from_clone; README and REVISION point to it. | repro-ci-4 |
| I53 | minor | CI gaps: generated outputs not compared, 'Label(s) may have changed' not checked | Both added to paper-build.yml. | repro-ci-5 |
| I54 | minor | Git history contains the AWS account id (b8b1a86) and a personal e-mail (69b1aa9) | Not changed: rewriting published history is the author's decision (reported). | repro-ci-6 |
| I55 | minor | Artifacts embed absolute local paths | Noted; kept as provenance. | repro-ci-7 |

## Completeness critic

| # | target | disposition |
|---|---|---|
| 1 | The stress test in §limits, the abstract (c4bf5f3 paper.tex:35-36) and the Introduction's third contribution (124-125), all \bmlStress* macros. Which checkpoint | Confirmed and fixed. The 14.9M-event stress suite was scored with the stage-5 checkpoint (aws/ud_bineval.sh), the released model's predecessor. The full suite is on S3; validation/stress_rescore_local.py regenerates the first shards of each quoted tier with the suite's seeds (and its original out-of-range class mix, run_shard --legacy-oor-mix) and scores the same events with both checkpoints. The paper now quotes the released checkpoint on that subset, with the suite's stage-5 values alongside (Table tab:stress), and says which model scored the suite; stage 5 on the subset reproduces the suite (macro-F1 0.927 vs 0.927, median difference 0.012). The released model's natural macro-F1 is 0.919 (its held-out 0.919). The 0.524 figure is confirmed not to be a single-lens recall (38% of the weighted PSPL labels are demoted binaries): sub-day single lenses are classified PSPL 0.274 and called anomalies 71% of the time (37% above the frozen threshold); the paper now says so, and that stage 6 trained on the edges of three of the sweeps. |
| 2 | Appendix app:infra 'Training recipe' and 'Simulation (AWS)' (c4bf5f3 paper.tex:1571-1587; 218e3f1 1653-1661), and the §limits framing 'parts of parameter space  | Confirmed and fixed. Sec. limits no longer calls the low-q regime barely populated (it is a stage-4 training pool) and states the stage-6 coverage of the sweeps. The appendix now describes the six-stage warm-start chain from the stage logs (base: batch 512, learning rate 3e-4, factor 2 on the anomaly class; stages 2-6: learning rates 1.2e-4, 3e-5, 5e-5, batch 384 from stage 4, factor 1; stage 6: 1.9M events, 6 epochs, zero-based epoch 4 released; ~22 h in total); paper/canonical_numbers.json infra corrected (batch 256 and 5 epochs had described the base run's defaults). |
| 3 | Whether the training caches of stages 4-6 are disjoint from the 450,589-event test pool (shards 90-149, base 20260720). Those caches are: 15 hard regimes, the w | Not checkable here (the training caches are on S3); AUDIT finding 14 stays open and is listed in the ledger. |
| 4 | The current state after verification. HEAD moved to 218e3f1 ('Third verification: code and text fixes (WIP; artifacts regenerated in the next commit)'): 27 file | Addressed: after the fixes the final state was checked again by a fourth verification (addendum below); macros, tables and rendered draft are now compared with the committed files in CI. |
| 5 | The shipped model's weighted 1S2L recall at the 5.2% budget, 0.144, which appears in the abstract and in Table 7. | Kept: 0.144 is the grid threshold's value (the budget sits 0.09 points off because of a tie block); the caption now states the weighted slack. |
| 6 | The RMDC26 partial-season cascade numbers (\bmlCg*): premature 2.2% [1.6,3.0], lag +6.5 d, detection 61% [59,64], three-band values, single-lens alerts 6.2% (0. | Checked in the fourth verification (addendum below): see its findings cascade-1 to cascade-7 and the ledger findings. |
| 7 | The new in-house seed spread (held-out AP 0.876-0.894 with pauses, 0.919-0.931 clean, across three seeds of one recipe) against in-house single-run comparisons  | Addressed where the paper interprets in-house single-run AP differences: the pauses-vs-random-gaps comparison (0.921 vs 0.911, clean 0.948 vs 0.952) is now stated to be within the 0.018 seed spread; other in-house comparisons (cadence, gap augmentation vs shipped) are either far larger or already called unresolved. |
| 8 | Checkpoint provenance of the other pre-RMDC26 artifacts that record no checkpoint hash: baselines_result.json, gap_matched_result.json, latency_gaps_result.json | Partly: the artifacts predate no checkpoint change after stage 6 (all committed 2026-08-04 or later, after stage 6 shipped on 2026-07-26), so stage 6 is likely but not recorded; noted in PROVENANCE, not changed. |
| 9 | Abstract length against the Astronomy & Computing limit. | Flagged to the author: the abstract is ~360 words (submitted ~265); A&C's limit was not checked offline. |
| 10 | The audited F087/F213 constants in ROMAN_BANDS_COLOUR_AUDITED (zeropoints, thermal backgrounds, saturation) and the §limits calibration sentences that precede t | Not checked (needs the current Roman calibration tables); the audited constants are as recorded in pipeline/photometry.py with their sources. |
| 11 | Row-by-row dispositions in paper/REVISION.md §1.5 (rows 1-22) and §2, checked against the final paper after 218e3f1 and the artifact commit. | Checked in the fourth verification (addendum below): see its findings cascade-1 to cascade-7 and the ledger findings. |
| 12 | What 'three training seeds' varies. The fine-tune's --seed also sets the train/val/test permutation of the shared mm_train, so each seed trains on a different 8 | Addressed in the text (the seed also sets the split and the kept epoch) and in PROVENANCE and REVISION row 20. |

## Left open

* Rewriting published git history to remove an old AWS account id and e-mail address (I54) is the author's call.
* The abstract is longer than submitted (I39); A&C's limit was not checked.
* Absolute local paths in artifacts (I55) are kept as provenance.

## Addendum: fourth verification of the final state (2026-09-13)

Six verifiers (the released model's stress numbers, the RMDC26 cascade reduced independently, the revision ledger,
the changed manuscript text, code/artifacts/CI, the appendix and documentation), one adversarial checker per slice
and a completeness critic checked the state after the fixes above (commit 9c04593). Raw record:
`docs/verification/2026-09-13_fourth_findings.json`; dispositions: `docs/verification/dispositions_2026_09_13.py`.

**62 findings: 0 critical, 16 major, 46 minor; adversarial verdicts 53 real, 9 partly, 0 refuted.**

The main corrections: the regenerated stress subset had used a fixed generator for the sub-day sweep (now regenerated
with the July generator's peak-time draw, and described as a new realisation of the suite's populations, compared
within itself); the faint sweep's anomaly precision reflected its class mix (the paper now quotes mix-independent
false-anomaly rates); the RMDC26 cascade is compared with the in-house scan stratum by stratum; the referee round's
per-event archive had never been committed; the appendix had described an earlier simulation run and an unsourced
benchmark; Sec. results had located the in-distribution misses at weak anomalies, where the artifact puts them at
wide separations.

| id | severity | verdict | finding | disposition |
|---|---|---|---|---|
| cascade-1 | major | real | RMDC26 premature/lag/detection for all 1,667 eligible events are set against the in-house giant and Neptune strata, called "the only strata comparable with RMDC26's planetary anomalies". The text says | Fixed. cascade_gulls.py reduces the RMDC26 timing by the in-house mass-ratio strata (offline, from the cached scan and the pinned meta parquet; every earlier value unchanged); the paper compares giant with giant and Neptune with Neptune (F146 and three bands) and states the 40% at q <= 1e-4 that the in-house scan samples with 9 events; REVISION row 10 corrected. |
| cascade-2 | major | partly | Data availability: "the other ablations and auxiliary experiments from their own archived JSON artifacts, each with the script that produced it. Re-deriving any reported statistic from these requires  | Fixed by disclosure: Data availability says the scan's traces are not distributed and regenerate only by rerunning the scan; the re-derivation claim names this exception. |
| code-1 | major | real | `--legacy-oor-mix` is said to 'reproduce the shipped stress suite bit for bit' and 'regenerate those shards exactly'. The paper (765, table caption 809) calls the subset the stress set's first shards  | Fixed with stress-01. |
| code-2 | major | real | Data availability says the referee round's per-event inputs (probability traces, scores, labels, weights and the two colour fine-tunes) 'are archived with their hashes in validation/referee_round_arch | Fixed with ledger-referee-archive-untracked. |
| docs-01 | major | real | "A base model was trained from scratch on 540,952 natural-prior events with AdamW (learning rate 3x10^-4, weight decay 0.01, batch size 512)". The 12b critic-#2 disposition, CHANGELOG:39-40 and REVISI | Fixed. Peak learning rates of one-cycle schedules from the checkpoints' optimizer states (base 4e-4). |
| docs-02 | major | real | "BinML was trained on roughly \bmlSimEvents (=10) million simulated events" (baselines paragraph). The Appendix says "The training set of roughly 10 million events was generated on Amazon Web Services | Fixed. The baselines paragraph says up to 1.9M events per stage; the simulation paragraph no longer describes the earlier 10M-event run (canonical_numbers notes it). |
| docs-03 | major | real | "A short fine-tune on the audited calibration (one seed) keeps AP and restores periodic-variable F1 on the audited photometry (0.957) but collapses on the old one (0.750)." | Fixed in all four docs. |
| docs-04 | major | real | "the fitted-PSPL baseline is limited to 800 of 6,912 F146 epochs" | Fixed (all methods see all 6,912 F146 epochs; residual AP 0.545). |
| docs-05 | major | real | "The full stress suite has 10.4M targeted out-of-distribution events plus a 4.5M same-prior subset" | Fixed with stress-08. |
| ledger-referee-archive-untracked | major | real | Section 2: 'per-event inputs archived with hashes in validation/referee_round_archive/'. Row 21 lists validation/referee_round_archive/ as an artifact. The paper's Data availability says the per-event | Fixed. The archive is committed (gitignore exceptions) and a test checks every hashed file is tracked and matches. |
| stress-01 | major | real | The first shards of each tier were 'regenerated ... with the set's seeds and class mix'; 'Stage 5 on this subset reproduces its full-set numbers'; 'under that set's class mix 38% of the weighted PSPL- | Fixed. run_shard --legacy-t0-pad reproduces the July generator's peak-time draw (tested: same random stream, kept t0); the three sweep tiers were regenerated with it (sub-day PSPL fraction 14.8% vs the suite's 14.75%; stage-5 PSPL recall 0.532 vs 0.524), the corrected sub-day tier is kept as a second row; 43% quoted for the suite's mix; 'bit for bit' removed from code, PROVENANCE, REVISION row 23 and CHANGELOG. |
| stress-02 | major | partly | 'Stage 5 on this subset reproduces its full-set numbers: ... a median difference of 0.012 over the quoted numbers, the largest (0.095) for the wide-separation recall, which rests on 60 events'. PROVEN | Fixed. The subset is described as a new realisation (unpinned fleet environment; local versions recorded in the artifact); the 2.3% anomaly deficit and stage 5's 0.956 vs 0.941 are stated, checkpoints compared within the subset (wide s: 0.328 vs 0.281); 'stands in' dropped; guards check the two largest differences and the deficit. |
| stress-03 | major | real | Faint-source anomaly precision falls to 0.026 'mostly because detectable anomalies are rare among faint sources (0.19% of events by weight, against 5.5% in the natural population): at the natural prev | Fixed. The faint sweep's precision is attributed to its class mix (4% binaries by weight vs 68%; detectable fraction 8.1% vs 4.9%, guarded); the paper, abstract and docs quote the mix-independent false-anomaly rates among microlensing events (3.3% vs 26%; 0.9% vs 12% above the threshold). |
| text-regeneration-not-bit-identical | major | partly | "we regenerated two shards of the held-out pool (15,016 events, disjoint from training; 15% of them are among the rows the frozen threshold was chosen on)"; Data availability: the training set "can al | Fixed. The colour paragraph says 75% of the regenerated events reproduce held-out events and 19% of those are threshold-selection rows (macros, guarded); Data availability says regeneration is statistically equivalent, not event-identical. |
| text-stress-subset-not-the-set | major | real | "For the released model we regenerated the first shards of each tier quoted here with the set's seeds and class mix ... Stage 5 on this subset reproduces its full-set numbers: macro-F1 0.927 against 0 | Fixed with stress-01/02. |
| text-subday-reconciliation-unsupported | major | real | "Sub-day single lenses ... are flagged as anomalies by the recommended checkpoint less often than in-support single lenses ... They are mostly shorter (median tE 0.13 d) than the 0.2--1 d single lense | Fixed with stress-04; guards for the median, the in-support comparison and the PeriodicVar majority. |
| cascade-3 | minor | real | "The in-house scan measured three bands only for its whole population, mostly stellar binaries (...), so its planetary strata cannot be compared on this point." REVISION row 10: "three-band in-house n | Fixed. cascade_reduce.py now reports the in-house three-band strata (cascade_reproduce_result.json regenerated through the validated reducer); the paper quotes them. |
| cascade-4 | minor | real | Directional sentences: "Alerts come later" (Lag > InGiantLag), "Detection is lower" (new in 218e3f1), "Revealing all three bands lowers premature alerts ..., lengthens the lag ... and lowers detection | Fixed. Guards for 'come later', 'lower', 'no more frequent', the three-band directions, the recalibrated directions, 'stay rare' and 'several times' (now >= 3x); perturbation cases added. |
| cascade-5 | minor | real | "The high-cadence season of 3,000 single lenses and 3,000 binaries was revealed ..."; "6.2% of single lenses per simulated event (8.9% weighted by event rate) raise an alert ..."; purity 5% / 21% / 49 | Fixed. Sample described as the first 3,000 scored single lenses and 1,500 of each planetary class; Wilson interval for the single-lens alert rate; the sample's lower full-season flag rate (4.8% vs 6.3%) stated as a slight optimism (guarded). |
| cascade-6 | minor | real | "RMDC26 contains no variable stars or flat sources (our synthetic ones rarely alert, \S\ref{sec:cascade}, but real variables are untested)" -- in a paragraph about the recommended gap-aware checkpoint | Fixed ('rarely alert for the shipped checkpoint ... not scanned with the recommended one'). |
| cascade-7 | minor | real | "On our own simulator, with continuous seasons and the same checkpoint, band and threshold, 31 of 1,759 generated single lenses alert (1.8%)" | Fixed in the text ('generated single lenses with a PSPL label', also in Sec. cascade, README, model card, evaluation.md); the artifact key keeps its name. |
| code-3 | minor | real | Every generator input is hashed in paper/results/MANIFEST.json and checked by validate_artifacts.py. | Fixed. inference_benchmark_result.json hashed; make_macros.py has an input registry and --list-inputs; a test checks its inputs against the manifest. |
| code-4 | minor | real | `code_generation_and_scoring` records the code state that produced the reused shards, caches and evaluations. | Fixed. Each tier writes WORK/stamp_<tier>.txt when generated and the artifact records it per tier; an empty code stamp is fatal. |
| code-5 | minor | real | referee_round.json and truth_relabel_impact.json 'were regenerated from a clean commit after the third verification'. referee_round.json stamps `code` = 8a8bd4b (clean). | Fixed. PROVENANCE lists which intermediates of referee_round.json were made at which commit; the 12b disposition I22 reworded. |
| code-6 | minor | real | The perturbation cases exercise the guards they are named for, and 'every new directional sentence has a fail-closed guard, and the guards are tested'. | Fixed. Every fail-closed case asserts the guard it is named for; the best-seed guard and the new cascade and sub-day guards have cases; the make_macros stress guards have value-perturbation checks. |
| docs-06 | minor | real | "stage 6 applied the truncation relabelling to every class" | Fixed (stage 6 relabels truncated binaries until the recorded onset). |
| docs-07 | minor | real | The recipe gives single learning rates per stage, calls the model 'the last of six warm-started stages', and for the fine-tunes gives only learning rates ('colour-calibration 5e-5, RMDC26 1e-4'). | Fixed (one-cycle schedule, base run's early stop, 'five of them warm-started', fine-tunes' batch 256 and factor 2). |
| docs-08 | minor | real | "Seeds are fixed (20260720) and the data loader is seeded per worker." | Fixed (stages loaded data in the main process; per-worker seeding added afterwards). |
| docs-09 | minor | real | "A benchmark of the training step (forward and backward pass on a full batch of 256) sustains about \bmlTrainEps (=958) events per second" | Fixed. The unsourced 958 ev/s benchmark is replaced by the logged per-epoch throughput (376-656 ev/s). |
| docs-10 | minor | real | "(the released model's last training stage also saw a short-timescale pool reaching 0.3 d, Appendix~\ref{app:infra}" | Fixed with text-appendix-xref-0p3d. |
| docs-11 | minor | real | Critic #4: 'after the fixes the final state was checked again by a fourth, smaller verification (addendum below)'. Critic #6 and #11: 'Included in the fourth verification.' | Fixed: this addendum. |
| docs-12 | minor | real | README: 'the released run is the best of the three seeds'; model card: '(the released run is the best of three on RMDC26)'; REVISION row 14: 'the released run is the best of three'. README:232-234: 't | Fixed (README, model card, REVISION row 14 narrowed to planetary recall). |
| docs-13 | minor | real | "Every table is regenerated by validation/gulls/gulls_summary_tables.py (from a clone: --scores validation/gulls/rmdc26_scores.csv.gz)" | Fixed (the notebook points to command_from_clone). |
| docs-14 | minor | real | The truth-label measurement is 'reported in the paper (§training, §limits)'; the 7.2-day label resolution is 'stated in §training'. | Fixed. |
| docs-15 | minor | real | Finding 14 status: 'lineage notes + the 4.5M unseen-natural stress result argue against test contamination'. | Fixed (finding 14's status cites the stage-4 log and the regenerated-subset result; the stage-6 shard indices remain the open item). |
| docs-16 | minor | real | "The shipped checkpoint followed a curriculum of targeted warm-starts (--init-weights) on harder data and then partial-season augmentation." | Fixed (training.md gives the stage order and calls the commands illustrative). |
| docs-17 | minor | partly | The 2026-09-12 entry says full-season-fit residuals 'taught 13.7% of truncated binary presentations NonPSPL'. The 2026-09-09 entry says gap-aware false alarms went '35.6% -> 11.7%'. The top entry says | Fixed (corrections added to the CHANGELOG entries; REVISION 'stage-6 recipe'). |
| docs-18 | minor | real | "one gap of 1-2 h placed at mid-season already lowers single-lens recall from 0.93 to 0.86 ..., the lost events going to NonPSPL and PeriodicVar" | Fixed ('going to NonPSPL', guarded). |
| docs-19 | minor | partly | Finding 10: '(chi^2 < 500; 18 of the 344 single-lens cases fail the amplitude floor)'. | Fixed (the unstored count dropped). |
| ledger-header-counts-stale | minor | partly | The section 1.5 intro attributes the rows as 'rows 18-19 by the 2026-09-12 re-verification, rows 20-22 by the referee round ...' and never mentions row 23. Line 42: 'the 22 rows of the ledger in §1½'. | Fixed (row 23 cited, 23 rows, status table refreshed). |
| ledger-row10-stale-wording | minor | real | 'single-lens alerts 6.2% of events per season (0.86 per 1,000 events per day); purity ... 5% at 1% planetary prevalence, 21% at 5%' and the bold conclusion '... detection does not transfer (as the ful | Fixed (units, weighted figure, per-event labels, stratified conclusion). |
| ledger-row22-section-label | minor | real | '✅ in paper (§training, §limits)' | Fixed (Sec. cascade, Sec. limits; also AUDIT, CHANGELOG, status table). |
| ledger-row7-stale-colour | minor | real | 'Removing F087/F213 lowers 1S2L recall at the matched 5.2% budget by 0.042 (g08e12), 0.067 (fspl_g08), 0.068 (fspl5s); 9.1-9.5% of frozen-threshold decisions flip. Reading withdrawn: on binaries with  | Fixed (recommended checkpoint added, 'only' dropped). |
| ledger-row9-host-visible-rounding | minor | real | 'host-visible subset 50% / 37%' | Fixed (51% / 37%). |
| ledger-rows6-11-not-updated | minor | real | Row 14 says the downstream analyses are 'now rerun for the recommended checkpoint (rows 3, 9, 10, 11, 15, sub-day)'. Row 6 still says 'fspl5s flags 0.7% (unweighted) / 3.1% (rate-weighted)'. Row 11 st | Fixed (rows 6 and 11 carry the recommended checkpoint's values). |
| ledger-subday-sentence-unlogged | minor | real | 'They are mostly shorter (median tE 0.13 d) than the 0.2–1 d single lenses of our own sweep, most of which the released model calls anomalies (§limits)' | Fixed (row 6 records the matched-timescale comparison). |
| ledger-unlogged-paper-changes | minor | partly | The ledger and the CHANGELOG's third-verification entry are the record of this round's changes; row 2 reports 'held-out AP under the measured pauses 0.921 vs 0.911 (clean 0.948 vs 0.952)' with status  | Fixed (row 2 qualifier; the three paper changes listed in the CHANGELOG). |
| ledger-verification-addendum-missing | minor | real | 'Addressed: after the fixes the final state was checked again by a fourth, smaller verification (addendum below)'. Completeness items 6 and 11 (item 11 is 'Row-by-row dispositions in paper/REVISION.md | Fixed: this addendum. |
| stress-04 | minor | real | RMDC26 sub-day single lenses (flagged 1.6%, mostly PeriodicVar) 'are mostly shorter (median tE 0.13 d) than the 0.2-1 d single lenses of our own sweep, most of which the released model calls anomalies | Fixed. The RMDC26 sub-day sentence now gives the matched-timescale contrast (released model 4.3% of RMDC26's 0.25-1 d single lenses vs 35% of our sweep) and says timescale alone does not explain it; guarded. |
| stress-05 | minor | real | Abstract: sub-day single lenses, 'most of which it calls anomalies'. | Fixed. The abstract quotes the operating-threshold rates (35% of sub-day single lenses; 12% of faint microlensing events without an anomaly, 0.9% natural). |
| stress-06 | minor | real | 'stage 6, trained after the stage-5 stress results, added pools at the edges of three of the sweeps: s = 3-8 ..., 0.3-10 d ... and source magnitudes 23.5-25'. | Fixed. Low-q and faint (23.5-25) pools entered at stage 4; stage 6 added more faint data and pools overlapping the wide-s and sub-day sweeps (s 3-8, tE 0.3-10 d); appendix and docs say so. |
| stress-07 | minor | real | 'PeriodicVar examples whose period exceeds the season are recovered as PeriodicVar only 0.039 of the time ... since within one window they are not observably periodic'. | Fixed ('comparable to or longer than the 72-day season (60-200 d)'). |
| stress-08 | minor | real | 'The full stress suite has 10.4M targeted out-of-distribution events plus a 4.5M same-prior subset'. | Fixed: 8.7M in 12 targeted regimes (mostly training pools) and 1.7M in 17 sweeps (README, evaluation.md, leakage_audit.md, model card; the CHANGELOG notes the earlier figure). |
| text-abstract-length | minor | partly | Abstract length after this round. | Flagged to the author (now about 380 words; A&C's limit not checked). |
| text-abstract-subday-and-faint-wording | minor | partly | "beyond the natural priors it fails, notably on faint sources (anomaly precision 0.026) and on sub-day single lenses, most of which it calls anomalies." | Fixed with stress-05. |
| text-appendix-xref-0p3d | minor | real | "the released model's last training stage also saw a short-timescale pool reaching 0.3 d, Appendix app:infra" | Fixed (points to Sec. limits; the appendix gives the pool ranges). |
| text-conclusion-submitted-version | minor | real | "Of the tests the submitted version listed as open, two are now reported" | Fixed ('an earlier version of this work'). |
| text-finite-size-cannot-matter | minor | real | "in the smaller bins, where finite size cannot matter, its reductions are within the training noise" | Fixed (physical claim removed). |
| text-pauses-at-least-as-well | minor | real | "Training on the pauses themselves does at least as well ... with clean average precision 0.948 against 0.952 ... on RMDC26 the measured pauses lead by more than that seed range." | Fixed ('as well within the seed spread'; 'lead in recall by more than the seed range of Table tab:seeds'). |
| text-seed-table-display-ties | minor | real | Caption: "entries in bold exceed [the seed range] at the same budget and weighting." | Fixed ('compared at full precision' in the caption). |
| text-stress-regime-descriptions | minor | real | "out-of-range sweeps that redraw one parameter of one class beyond the natural priors"; "PeriodicVar examples whose period exceeds the season" | Fixed. |
| text-truncation-label-wording | minor | real | "a conservative bias that almost always delays the label (it can advance it where detectability is not monotone ...)"; "the disagreement for binaries falls to 2.4%, the cases where detectability is no | Fixed ('usually delays', 212 of 241, guarded; the fixed rule's 2.4% attributed to both sources without a proportion). |

### Completeness critic (fourth verification)

| # | severity | target | disposition |
|---|---|---|---|
| 1 | major | paper/paper.tex:593-595, 628-631 and 716-723 at 9c04593 (Sec. results and Fig. plane), which describe where the released model misses detectable anomalies | Confirmed and fixed. make_figures.py now derives where the misses sit from the frozen artifact: 55% of misses are at s > 2.9 (11% of detectable anomalies), 46% have anomaly dchi2 > 1e4; argmax recall 0.744 vs 0.974, completeness 0.602 vs 0.913; the plane's widest stellar-ratio cells 0.69-0.96. Sec. results rewritten from these macros (guarded). |
| 2 | minor | Core held-out numbers: abstract and Sec. results, Tables 3, 4 and 5, confusion, ECE/Brier, AP against the oracle, prior shift (Fig. 10), relabelling percentages | Fixed: 'across the evaluation pool'; the two false-positive diagnostics now have their own macros (94.4% of NonPSPL false positives; 95.0% of PSPL-to-NonPSPL errors). |
| 3 | minor | paper/paper.tex:630, 646, 723, 740 and 838: metrics switch between argmax recall and completeness at the operating threshold without saying so | Fixed: argmax recall labelled as such where it sits next to threshold completeness. |
| 4 | minor | paper/paper.tex:139 (Fig. 1 caption): 'Only bands with surviving epochs are plotted; all six examples here retain all three' | Fixed (caption: the periodic-variable example has no F087 epochs). |
| 5 | none | LaTeX build, rendered layout (Table tab:stress, stuck floats), macro regeneration, test suite | Checked, fine. |
| 6 | none | Every-class stream numbers quoted in the abstract, Sec. cascade and the conclusion, and the in-house single-lens comparison in Sec. gulls:cascade | Checked, fine. |
| 7 | minor | Discussion and Sec. gulls:seasons prose without macros: paper.tex:1470-1472 (latency by mass ratio) and 1495-1496 ('scorable at a false-alarm cost below one per | Fixed ('about one percent, at most 1.6% at 95% confidence'). |
| 8 | minor | paper/paper.tex:1093: '82% of our NonPSPL evaluation events are stellar-mass-ratio binaries (median q = 0.12)' | Fixed: the RMDC26 comparison uses the final test's detectable anomalies (82%, median q 0.13). |
| 9 | minor | paper/paper.tex:139 and 312-314 (F087 undetected for 21.6% of detectable anomalies) and 838 (no-blue-band recall 0.918) | Disclosed: Data availability names the no-blue-band slice as read from metrics.json (per-event band counts not saved). |
| 10 | minor | paper/SUBMISSION_FIELDS.txt (plain-text abstract and highlights for the A&C form; untracked, dated Aug 15) | Flagged to the author: paper/SUBMISSION_FIELDS.txt (untracked) still has the submitted abstract; regenerate it from the current abstract before resubmitting. |
| 11 | none | Sec. model and Table tab:simulation constants against the code; draft mirror; Table tab:gulls spot check | Checked, fine. |

Left open after the fourth verification: the abstract's length (about 380 words) and the plain-text abstract in
the untracked `paper/SUBMISSION_FIELDS.txt` (the author's, before resubmitting); AUDIT finding 14 (the stage-6 cache's
shard indices, on S3); the audited F087/F213 constants against the current Roman tables (network); rewriting published
git history (I54).
