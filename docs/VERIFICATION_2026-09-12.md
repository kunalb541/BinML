# Second verification pass of the revision work (2026-09-12)

Five independent verifiers (macros, draft logic, new results, code and tests, docs and ledger) re-derived the
corrected state at commit 84216cb and later; each slice's findings were then checked by an adversarial
checker, and a completeness critic examined what nobody had covered. Raw record:
`docs/verification/2026-09-12_findings.json`; dispositions: `docs/verification/dispositions_2026_09_12.py`
(this file is generated by `docs/verification/build_doc_2026_09_12.py`, which asserts that every finding is
mapped to exactly one issue and that each issue's severity is its worst finding's).

**74 findings: 6 critical, 22 major, 46 minor; 0 refuted.** They group into 47 issues.

## What changed as a result

* **The recommended gap-aware checkpoint changed.** Weighting RMDC26's events by rate (which the draft had not
  done or disclosed) puts the combined arm (finite-source single lenses + the measured-season pauses) ahead of
  round 3 at every matched budget; per simulated event the two tie, and on our own held-out the combined arm is
  better. It is now recommended (`ft_fspl5s_seasons_g08.pt`, threshold 0.956) and the operating point, label,
  floor, sub-day, occupancy, between-season and cascade analyses were rerun for it.
* **Two claims were wrong.** The combined arm's extra false alarms are spread over every rho/|u0| bin (not the
  largest ones), and the finite-source physics removed a further part of the false alarms, not most of them.
* **The macro generator is now genuinely fail-closed** (tested), rounds once from counts, and counts every
  checkpoint scored on RMDC26.
* Code: caches keyed by weights, curves stored for local rescoring, onset window end, generation settings
  carried into the memmap, legacy ESPLMag2 regimes, stale-output bug in the fine-tune runner.

## Issues

| id | severity | issue | disposition | findings |
|---|---|---|---|---|
| H01 | critical | Combined arm's extra false alarms said to be 'mostly in the largest-ratio bins' (draft, ledger row 14) | Fixed: 39 of the 468 extra flags are in the two largest bins; the draft now says 'in every rho/|u0| bin' and row 14 says most extra flags are at rho/|u0| < 1. | macros-1, draft-logic-1, new-results-1, docs-ledger-1 |
| H02 | critical | Physics credited with 'a large part' of the residual false alarms; 'point-like bins barely change' | Fixed: draft, ledger row 13, CHANGELOG and G14 now say the extra training did most of it; the physics lowers false alarms in every bin (-14% to -44%), most in rate in the largest bins, part of it where finite size cannot matter; weighted, its recall gain is unresolved. | draft-logic-2, new-results-2, macros-12 |
| H03 | critical | All RMDC26 rates were per simulated event without saying so; conclusions depend on it | Fixed: weighted blocks, per-checkpoint operating points and paired bootstrap differences in transfer_tradeoff_all.json; two weighted columns in Table gulls; a weighting paragraph in Results; the timescale mismatch disclosed; the recommendation re-decided under both weightings (the combined arm, which ties per event and leads weighted, is now recommended) and every downstream analysis rerun for it. | draft-logic-3 |
| H04 | major | Macro generator not fail-closed: tables written before later checks; four silent fallbacks | Fixed: outputs built in memory and written only after every check; need() replaces the fallbacks; 'FATAL' rejected as a value; duplicate macros rejected. tests/test_macros_fail_closed.py mutates six inputs and checks that nothing is written. | macros-2, code-tests-1, draft-logic-15 |
| H05 | major | Checkpoint count (8, then 9) excluded five scored schedule arms; documents said 7 | Fixed: \bmlGullsNcheckpoints counts every fine-tune scored on RMDC26 (14); G44 and the first-pass summary updated. | macros-3, draft-logic-10, code-tests-2, docs-ledger-10 |
| H06 | major | Double rounding: mean recall 0.538 (exact 0.5385 -> 0.539), colour undetectable 0.170 (383/2260 = 0.169), several ledger and section-1 values | Fixed: unrounded mean recall stored and used; colour and occupancy counts stored; between-season percentages from counts; ledger rows 3, 5, 13 and section 1 values rounded once. | macros-4, docs-ledger-2, new-results-5, docs-ledger-6 |
| H07 | major | The 0.014-0.023 'seed spread' is two runs of one recipe on different pools with the same seed | Fixed: described as a pool spread in the draft and ledger; its weighted analogue (0.005) added; the text no longer calls it run-to-run or seed variation. | macros-5, new-results-11 |
| H08 | minor | 'generalises poorly to the other five (0.568)' but 0.568 is over all six seasons | Fixed: 'scores only 0.568 across the six seasons, its own included'. | macros-6 |
| H09 | minor | Reversed range '23--21%' | Fixed: min/max range macros for the bright-baseline and blend-fraction ranges. | macros-7 |
| H10 | minor | Caption said the budgets equal two checkpoints' rates | Fixed in 0988a0d ('close to ... 5.2% and 11.6%'). | macros-8 |
| H11 | minor | Typed data-derived numbers in a file that claimed none | Fixed: season shortfall, recurring pause days and the pooling sensitivity are macros (new dataset facts); the header now says typed numbers are method constants only. | macros-9, draft-logic-14 |
| H12 | minor | Partial-occupancy share quoted as 34% (minimum season) vs 35% elsewhere; colour 2/3 share '45%' | Fixed: median season (35%) in the draft; ledger row 15 gives 34.5-35.3% and 36-47% (about 41%) for colour bins. | macros-10, new-results-12, docs-ledger-11 |
| H13 | minor | Table captions: gap-table first row is not a fine-tune; missing tablenotemark; omitted bins unstated; wrong section reference | Fixed: captions reworded, tablenotemark added, the two omitted rho/|u0| bins named, the relabel reference points to Sec. limits. | macros-11, draft-logic-13 |
| H14 | minor | schedule_finetune.json lacked n_true_class_binaries and recorded the code at write time | Fixed: runner records code_at_start and code_at_write; the reduce was rerun from cached results (numbers unchanged, 28,419 now stored). | macros-13, new-results-6 |
| H15 | minor | render_draft.py fails on a clean clone (paper_macros.tex is generated and gitignored) | Fixed: clear error with the build order (make_figures, make_macros, make_gulls_macros, render_draft). | macros-14, code-tests-14 |
| H16 | major | Cascade three-band comparison used the whole in-house scan against planetary strata | Fixed: the draft says in-house three-band numbers exist only for the whole, mostly stellar, scan and are not comparable with the planetary strata; ledger row 10 likewise. | draft-logic-4 |
| H17 | major | Discussion: 'recovers most' of the between-season events (scorability, not recovery) | Fixed: 'makes most of the former scorable ... but at the frozen threshold flags only 20% of their planetary events'. | draft-logic-5, new-results-3 |
| H18 | major | 'a fifth of the selected events' pools classes, mixes eligible/selected, omits RMDC26's t0 concentration | Fixed: class split (27% of single lenses, 9-10% of planetary events between seasons; 23% and 4-9% in low-cadence seasons), 'eligible', and the concentration of catalogue peaks in high-cadence seasons (64-71% in 25% of the span), all from rmdc26_dataset_facts.json. | draft-logic-6 |
| H19 | major | 'the gap-aware checkpoint of Sec. gulls' in [B] meant g08e12 in Sec. gulls | Fixed: [B] now points to 'the recommended checkpoint'; g08e12 is 'the gap-augmented checkpoint' throughout the draft. | draft-logic-7 |
| H20 | major | Clean-data cost stated in macro-F1 for one arm and AP for the other | Fixed: average precision for both, with the direction stated. | draft-logic-8 |
| H21 | major | The paper's own single-slice threshold (0.931 for fspl5s) was not disclosed | Fixed: the operating-point paragraph gives the single slice's threshold and its RMDC26 rates for the recommended checkpoint (0.946: 3.2%, 0.32) and why the full pool is used. | draft-logic-9 |
| H22 | minor | 'limited by significance rather than amplitude' not supported by a stored 2-D breakdown | Fixed: 'tracks significance more closely than amplitude' (the verifier's 2-D check supports it). | draft-logic-11 |
| H23 | minor | Citations: GBTDS citations dropped from [B]; RMDC26 not cited or expanded at first use; baseline offset in main text | Partly fixed: GBTDS citations restored, RGES-PIT cited at first use in [B], the baseline remark removed from the text. The expansion of 'RMDC26' is not documented anywhere we could find; the draft names it 'the RMDC26 release' and the [F] block asks the author to confirm it with the RGES-PIT. | draft-logic-16 |
| H24 | minor | Hedges: 'because' in the abstract; untested wing-season explanation; sub-day 'not as microlensing'; prevalence and weighting undefined | Fixed: 'mostly because'; '(we did not test why)'; sub-day now 'most probable class mostly PeriodicVar, 15% microlensing' and compared with in-support rates; purity stated as single-lens contamination at a stated prevalence. | draft-logic-17 |
| H25 | major | Ledger row 9 quoted an in-season comparator (0.48 / 0.57) that no artifact holds | Fixed: 0.50 / 0.58 for fspl5s and 0.53 / 0.62 for the recommended checkpoint, from the relabel artifact. | new-results-4, docs-ledger-3 |
| H26 | minor | A fourth difference of the combined arm (finite-source visible amplitude in the truncation augmentation) not named | Fixed: the draft and row 14 name it; the smooth-function control carries it too, so 'these two changes together do nothing measurable'. | new-results-7 |
| H27 | major | floor_sensitivity.json's calibrated block was at the superseded 0.949 and not reproducible by the committed script | Fixed: regenerated (label side unchanged); calibrated blocks now store their threshold; command recorded; PROVENANCE row updated. | new-results-8, code-tests-3, docs-ledger-7 |
| H28 | minor | seasonsocc artifact labelled 'seasons'; calibration eval directories renamed after evaluation | Fixed: label corrected by hand with a note field; renames documented in PROVENANCE (logits reproduce exactly). | new-results-9, code-tests-8 |
| H29 | minor | train.py docstring quoted 5.4% -> 4.4% for occupancy; run_shard comment implied a fixed pool | Fixed: 4.9% -> 3.6% with the artifact named; the control is 'a different random realisation of the same priors'. | new-results-10, code-tests-9 |
| H30 | minor | Generation settings (onset, noise, regime, ESPL function) stopped at the raw shards | Fixed: run_shard writes espl_function; build_cache and to_memmap carry gen_settings into the cache attrs and meta.json; tests/test_gen_settings.py. | code-tests-5 |
| H31 | minor | fspl_finetune_local.py reused stale eval/RMDC26 outputs after a retrain; provenance hard-coded ESPLMag | Fixed: a stamp change deletes the eval dir, rows and summaries; espl_function derived from the prefix. | code-tests-6 |
| H32 | minor | transfer_subday.json lacked calibrated entries the committed script now writes | Fixed: regenerated with the recommended checkpoint added (existing values unchanged); command recorded. | code-tests-7 |
| H33 | minor | _visible_amplitude comment said binaries never reach the finite-source branch | Fixed: comment names the fspl5 regimes (binary rho up to 0.1). | code-tests-10 |
| H34 | minor | Fine onset grid never tested the window end when res does not divide 72 d | Fixed: the window end is the last cut; regression test added. | code-tests-11 |
| H35 | minor | Cascade and between-season caches keyed by model name, not weights | Fixed: manifests bind names to weight hashes and refuse collisions; the cascade reducer merges per-model scan files; the between-season extractor stores curves and rescores missing models locally. | code-tests-12 |
| H36 | minor | CI extras lacked pyarrow/pandas, so the scores-route test always skipped | Fixed: added to the dev extras. | code-tests-13 |
| H37 | major | The legacy ESPLMag2 flag could not be set without editing code; the Reproduce block rebuilt a different pool | Fixed: regimes fspl5s_legacy{,_highmag} (same seeds, ESPLMag2); runner docstring and Reproduce block updated. | code-tests-4 |
| H38 | major | CHANGELOG second entry repeated three withdrawn phrasings | Fixed: 20.1% of NonPSPL-labelled events (7.4% of binaries); 'first season's seven pauses'; 'no anomaly our policy would claim within one season'. | docs-ledger-4, code-tests-15 |
| H39 | major | PROVENANCE said the quantised onset affects only post-submission fine-tunes | Fixed: it is shared by the shipped model's training labels (disclosed in the paper). | docs-ledger-5 |
| H40 | minor | 13 old transfer artifacts carry the obsolete 'NOT A TRANSFER MEASUREMENT' status string | Documented in PROVENANCE (artifacts left as written). | docs-ledger-8 |
| H41 | minor | Section-1 numbers (6 of 89,919; 0.4%) exist only in the first pass's notes | Fixed: attributed to docs/verification/2026-09-11_findings.json, the 0.4% labelled a proxy measured on the cadence pool. | docs-ledger-9 |
| H42 | minor | Superseded ledger's closing paragraph read as current; blank lines broke its table | Fixed: labelled superseded; table joined. | docs-ledger-12 |
| H43 | minor | Stale comments and docstrings (first-pass G28 fixes not made) | Fixed: gulls_transfer.py, gap_sensitivity.py, train.py, binml/gulls.py, multi_season.py, tests/test_gap_augmentation.py. | docs-ledger-13 |
| H44 | minor | Section 1: 'no other class lost' for fspl_g08 (LongPeriodVar lost 0.005-0.013) | Fixed. | docs-ledger-14 |
| H45 | minor | Status-table row cited the superseded fspl_g08 calibration file | Fixed: cites gapped_threshold_fspl_g08_seasons.json. | docs-ledger-15 |
| H46 | minor | Missing bibliography entries tracked only in a draft comment | Fixed: rows in the REVISION status table (author action). | docs-ledger-16 |
| H47 | minor | Ledger/draft inconsistencies (0.325 vs 0.326, 0.298 vs 0.299, 'Timing transfers') | Fixed with H06 and H16; row 10 now says premature alerts stay rare and alerts come later. | draft-logic-12 |

## Completeness critic

| # | target | disposition |
|---|---|---|
| 1 | Per-event RMDC26 scores (gulls_curve_cache/rows_full_*.json, validation/gulls/rmdc26_scores.csv.gz) for every checkpoint except fspl5s_g08.  | No problem: every committed score reproduces from the rows; FA counts match (g08e12 off by one event at 4 d.p. storage). |
| 2 | The abstract/[C] attribution that the shipped model flags 35.6% 'because that simulation's F146 observing pauses are absent from our trainin | No problem for the attribution; the occupancy nuance (single lenses read occupancy as anomaly only for fspl5s) is consistent with row 15. |
| 3 | The in-house cascade strata quoted in the draft and ledger row 10 (giant n=138: 1.4% premature, 89% detected, +4.5 d; Neptune n=48: 4.2%, 73 | Strengthens H16: per-stratum three-band numbers diverge in-house (Neptune detection falls); handled by H16. |
| 4 | Draft [B], README and model card: 'A single gap of half an hour costs nothing, but one gap of 1-2 h already lowers single-lens recall from 0 | Position dependence of the single-gap cost: README, model card, REVISION section 1, the draft and gap_sensitivity.py now say the gap sits at day 43 and the cost depends on position. |
| 5 | Audit finding 50: P(NonPSPL) is stored at 4 d.p. and compared with 16-digit thresholds (0.9042405..., 0.9569272...), and the 4-d.p. threshol | No problem: 4-d.p. storage moves no macro. |
| 6 | 'Two fine-tunes with the same recipe on different pools differ by 0.014-0.023' (sched_rand vs g08e12). Nobody compared the two recipes (new- | Adds evidence to H07 (same-seed training is nearly deterministic here; seed variance unmeasured). |
| 7 | gulls_noise_model.json, the source of the draft's '2.64 times ours at 24-25 mag' and 'a 7-fold background variance'. It is still computed fr | Noise model computed from truth cache v1: ledger row 5 notes the v2 values (2.67x, 7.6x); conclusions unchanged. |
| 8 | Draft [C]: 'colour visits displace one of eight F146 epochs in about 34% of the 2-h bins'. The macros slice could not attribute partial bins | No problem: the partial bins are colour visits (78%) and pause edges; handled with H12. |
| 9 | First-pass fixes that dispositions.py records as done in code comments: G28 ('Fixed in paper/REVISION.md and code comments', covering datase | Stale code comments: fixed under H43. |
| 10 | Train/held-out disjointness of the calibration and gap-table pools. The regime name is not part of the RNG seed (audit item 26 'regime-name- | No problem: no train/held-out seed overlap. |
| 11 | Supporting facts nobody re-derived: the catalogue-baseline offset that justifies the empirical baseline ([C]; binml/gulls.py '~0.47 mag'); P | No problem: catalogue-baseline offset 0.471 mag reproduces (now stored in rmdc26_dataset_facts.json). |
| 12 | Whether the in-house cascade trace (the paper's premature-alert headline) regenerates at HEAD, given VERIFICATION line 134's 'validation/cas | No problem at the default worker count; the one-per-thousand statement traces to one seed. |

## Not refuted, not changed

Nothing was left unaddressed. One item is only partly done: the expansion of 'RMDC26' (H23) is not
documented in any source we found and awaits the RGES-PIT.
